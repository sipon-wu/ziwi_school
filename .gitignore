# OS
.DS_Store
Thumbs.db
code/.DS_Store

# Go build
**/backend/server
*.exe

# IDE
.idea/
.vscode/
*.swp
*.swo

# Dependencies
node_modules/
frontend/node_modules/

# Build
frontend/dist/
backend/tmp/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Go
backend/vendor/

# Environment
.env
*.env.local
.env.demo
.env.production

# Docker
docker/data/

# Logs
*.log
logs/

# CodeBuddy
.codebuddy/plans/

# Playwright 调试产物（运行时自动生成，非源码）
.playwright-cli/

# Local backups (large tarballs, never commit)
code_backup_*.tar
*.tar.gz.bak

# ── 2026-09-03 分拣 ──────────────────────────────
# Go 编译产物（体积大，非源码）
code/backend/full
code/backend/decor
code/backend/courseware

# 敏感凭据（含明文口令，禁止入库）
qa/dazhou_credentials.json

# 运行/构建产物
code/ai-service/output/
out_h5_dazhou/
qa/dist-h5/
qa/downloads/

# 测试/回归报告（每次运行都会变，产物不入版本库）
api_regress_report.json
qa/api_regress_report.json
qa/e2e_full_report.json
smoke_report.json
qa/API回归过程_20260710.md

# 仿真数据产物
qa/dazhou_lessons.json
qa/dazhou_courseware_plan.json
qa/seed_dazhou_staging.sql
code/ai-service/scripts/plan_science_5.json
code/ai-service/scripts/plan_staging_35.json
code/ai-service/scripts/plan_staging_h5_30.json

# Playwright DOM 调试快照
code/login.yaml
code/staging-home.yaml

# 演示/打包产物（二进制或重复，不入版本库）
《观潮》PPT课件-优化版.pptx
《观潮》PPT课件-真课件版.pptx

# 微媒工作台移交包：Axure 原型参考导出（02_原型参考 含 583 张 PNG + html/css/js 共 800 文件）
# 属设计参考资料、非平台代码亦非平台消费的公共资源；保留在本地供查阅，zip 为同一份的冗余打包。
产品规划/微媒工作台_移交包/
产品规划/微媒工作台_移交包.zip

# 装饰元件 SVG 图源（第三方 Tabler + RemixIcon 派生，13M/3182 文件）
# 不入库以保持仓库精简；由 deploy.sh 随 ai-service/ 一并 rsync 到服务器，
# 生产灌库（seed/decor ingest）读的是服务器上的这份。
# 若 fresh clone 后需重建：从 Tabler / RemixIcon 上游重新拉取到该目录。
# 注意：assets/index.json 与 assets/tags.json 是自有的分类标注，必须入库。
code/ai-service/assets/svg/

# 本地安全归档（删除操作的可逆备份，不入库）
.archive/
