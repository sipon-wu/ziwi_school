# 出题模块崩溃修复与回归报告（2026-07-08）

## 前置：环境一致性核对
- 先核对 staging（school1.ziwi.cn）与 prod（school.ziwi.cn）已部署前端是否一致：
  - 文件清单、体积、index.html 引用的 asset hash（`index-BI6kB25-.js`）**完全一致** → 两边代码同源同版本。
  - 运行环境差异（prod=HTTPS:8080 / staging=HTTP:8081）为设计预期，非代码版本差异。
- 结论：**一致**，回到 staging 按 B 根治修复后，验证通过再同步 prod。

## 修复清单（仅前端，未动后端/库）

### 1. KnowledgeGraph.tsx — G6 v5 兼容崩溃（用户原始报障，B 根治）
- 根因：`@antv/g6 ^5.1.1`（v5 实例），但 284 行 `graph.draw()` 是 **G6 v4 遗留 API**，v5 实例无该方法；且调用发生在异步重绘路径中、绕过外层 try/catch，抛出未捕获异常 → 出题页右侧图谱崩溃。
- B 根治改动：
  - 删除 `graph.draw()`（v5 中 `setElementState` 已触发重绘，此行多余）；
  - 两处 `graph.render()` 加 `.then(() => renderedRef.current = true).catch(() => {})`，防止异步未捕获异常；
  - 新增 `renderedRef` 守卫：主 effect 重建前复位，渲染完成后置位；选中态更新 effect 加 `!renderedRef.current` 判空 + try/catch，避免对未就绪实例调用方法。

### 2. ExerciseEditor.tsx — 回归中发现的预存独立 bug（顺带修复）
回归 `/exercises/:id` 时发现与 G6 无关的两层预存缺陷（prod 同样存在）：
- **bug A**：第 1 行 `import { useState, useMemo } from 'react'` **漏导入 `useEffect`**，但 35/57 行使用了 `useEffect` → `ReferenceError: useEffect is not defined`。
- **bug B**：第 92 行 `const [saveMsg, setSaveMsg] = useState('')` 声明在 `if (loading)` / `if (!question)` 两个 early return **之后** → 不同渲染 hook 数量/顺序不一致 → `React #310 Invalid hook call`。
- 修复：补 `useEffect` 到 import；将 `saveMsg` 的 `useState` 上移到 early return 之前（与其他 hook 同级）。

## 验证（Playwright 模拟点击，教师账号）

### staging（school1）出题模块回归
- `/exercises/new`：**PASS**（pe=0, appError=false, 可见；canvas 点击触发选中态路径无崩溃）
- `/exercises/:id`（真实 id acdd6271）：**PASS**（pe=0, appError=false, 可见，内容正常渲染）

### staging 全页面冒烟（21 路由）
- **FAIL=0，pageError=0 全页无崩溃**；10 个 WARN 均为非致命 console 错误（如教师访问 /it-admin 的 403），与原始 prod 表现一致。
- 结论：G6 修复未引入任何回归。

### prod（school.ziwi.cn）出题模块验证
- `/exercises/new`：**PASS**（pe=0, appError=false, 可见）
- `/exercises/:id`：对该测试 id（95085d91）**pe=0、appError=false（未崩溃）**；`visible=false` 系该 id 在 **prod 库不存在**（API 返回 404，页面优雅显示"题目不存在或已删除"），属 staging/prod 数据差异，非代码缺陷。同 build 在 staging 用真实存在 id 已 PASS。

## 发布
- dist 经 `rsync --delete` 同步至 `/var/www/school.ziwi.cn/`（仅前端，未动后端/库）。
- prod index.html 时间 `2026-07-08 23:05:34`，asset hash 与 staging 同源（`ExerciseEditor-BXek_1Zz.js` 等）。
- prod 与 staging 现再次一致。

## 备注
- 校验脚本对 KnowledgeGraph chunk 的 `draw()` 子串匹配为误报：该 `draw()` 来自 G6 库内部（如 `this.context.element.draw({type:'render'})`），是 v5 正确用法，非我删除的 `graph.draw()`。staging 已确认运行时不再抛 draw 类异常。
