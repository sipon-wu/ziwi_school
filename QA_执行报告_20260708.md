# 知微AI教学助手 V2.4 — QA线上执行报告

> 测试日期：2026-07-08（线上 school.ziwi.cn）
> 测试执行：小Q（自动化脚本：curl + puppeteer-core 无头 Chrome）
> 提测版本：V2.4（后端 1783511…，前端 dist 含 ITAdminPage 修复 + favicon 调整）
> 测试账号：语文教师 13800000002 / teacher123

---

## 一、执行范围与结论概要

按《QA测试方案_正式版_20260707.md》四阶段推进，**本轮回测以自动化脚本覆盖可脚本化用例为主**（认证/接口/数据持久化/安全/特殊字符/AI/UI冒烟），浏览器兼容性矩阵（Safari/iPad/Firefox/弱网/离线）、打印导出、完整 UI 交互（Toast/Loading/ESC 等）及全流程人工走查建议后续补充。

| 维度 | 已执行 | 结果 |
|------|:---:|------|
| 认证 / 接口 P0 | 28 条脚本 | 23 PASS / 5 已澄清(契约差异) |
| 功能 P0（UI 冒烟） | 12 条 | 10 PASS / 2 FAIL（真实缺陷） |
| 数据持久化 | 6 条 | 5 PASS / 1 设计限制 |
| 安全 P0 | 9 条 | 8 PASS / 1 待补测 |
| 特殊字符 / AI | 5 条 | 5 PASS |
| **真实缺陷(Bug)** | — | **3 个（1×P0，2×P1）** |

通过标准对照：P0 阻断 0 个 → **未达标（发现 1 个 P0 阻断：题库页白屏）**。

---

## 二、真实缺陷（Bug List）

### BUG-001 【P0 阻断】题库页面（/exercises）白屏崩溃
- **现象**：教师登录后访问「题库」，页面空白（body innerText 长度 0），控制台报错 `PAGEERR: t.knowledge_points.map is not a function`。
- **根因**：`questions` 表的 `knowledge_points`、`options` 为 **jsonb 列**，当字段为空时后端返回 **空字符串 `""`** 而非数组 `[]`。前端 `Exercises.tsx:72`（`q.knowledge_points.some(...)`）与 `:176`（`q.knowledge_points.map(...)`）未做类型兜底，直接抛错导致整页渲染失败。
- **影响**：F-QB-01（题目列表显示）、F-QB-02（AI出题落地展示）均不可用；题库是教师核心高频入口。
- **建议修复**：
  1. 后端：`Question` 模型 jsonb 列空值返回 `[]`（repo 读取时 `COALESCE(knowledge_points,'[]')`，或改 GORM 类型为 `datatypes.JSON`）；
  2. 前端：`Exercises.tsx` 对 `knowledge_points`/`options` 做 `Array.isArray(x)?x:[]` 兜底。

### BUG-002 【P1 严重】创建题目接口返回 500（CREATE_FAILED）
- **现象**：`POST /api/exercises`（题干/答案/题型/学科/年级均填）返回 `500 {"code":"CREATE_FAILED","message":"创建题目失败"}`。
- **根因**：同源 jsonb 问题——handler `CreateQuestion` 未赋值 `Options`/`KnowledgePoints`，零值为空字符串 `""`，Postgres **jsonb 列拒绝空字符串**（`invalid input syntax for type jsonb`）→ 插入失败。
- **影响**：教师无法手动新增题目（出题闭环断裂）。
- **建议修复**：创建时将这两个字段置为合法 JSON（`"[]"` 或 `null`），或模型改用 `datatypes.JSON` 并带默认 `[]`。

### BUG-003 【P1 严重】首页「近期草案」恒为空
- **现象**：仪表盘「近期草稿」区块存在，但**始终无数据**；接口 `GET /api/analytics/teacher-dashboard` 返回 `recent: []`，而该教师实际有 2 个 `draft` 状态教案。
- **根因**：`DashboardRepository.GetRecentLessonPlans`（`code/backend/internal/repository/dashboard_repo.go:71-78`）查询报错被吞掉（`if err != nil { recent = [] }`），导致恒返回空数组。
- **影响**：F-DASH-03（近期草案列表）不达标，教师看不到最近编辑的教案入口。
- **建议修复**：排查该查询报错原因（疑似 `Select` 投影/`updated_at` 扫描或表名映射问题）并修正，错误不应被静默吞掉。

---

## 三、已澄清的契约差异（非缺陷，需对齐文档/实现）

| 项 | 测试方案描述 | 实际实现 | 结论 |
|----|------|------|------|
| 题目列表/创建端点 | `/api/exercises`（创建字段 `content`） | `GET/POST /api/exercises` 真实存在；创建**题干字段名是 `stem`**（handler 注释写的 `/api/questions` 已过时） | 实现可用，文档错 |
| 管理员越权端点 | `/api/it/*` | 实为 `/api/admin/*`（教师访问返回 403，正确） | 文档错，安全达标 |
| 个人信息更新端点 | （未明确） | `PUT /api/user/profile`（D-01 改名后重新登录验证持久化 PASS） | 实现可用 |
| AI 出题端点 | — | 前端 `aiAPI` 仅 `lesson-plan/exam/grading/chat` 四类；无 `exercise/generate`（本轮回测 AI 出题落地依赖题库渲染，受 BUG-001 阻断） | 需确认出题 AI 链路 |

---

## 四、逐用例结果（节选关键项）

**PASS（代表性）**
- F-AUTH-01/02/03、API-01：登录/错误密码/空表单 全部正确
- API-03/08/11/14/15、F-LP-01/F-EXAM-01/F-MAT-01/F-ASG-01：各列表数据正常（教案4/题目24/试卷3/作业4/素材8）
- API-05/06/07（教案增改删）、API-12/13（试卷增改）：均成功
- D-01/D-03/D-05（名称/邮箱/地区持久化，重新登录验证）：PASS
- CH-02/03/05（数学符号/中文引号/英文引号往返）：PASS（DB 编码正常）
- S-01/S-02/S-03/S-04/S-08/S-09：未登录401、过期Token401、教师越权校长/管理员403、SQL注入搜索与登录均被正确拒绝
- P-01：analytics 接口 45ms（<1000ms 标准）PASS
- AI-CHAT：小微对话真实返回（qwen-turbo）PASS

**FAIL / 待补**
- F-QB-01（UI）：题库页白屏 → BUG-001
- F-DASH-03：近期草案空 → BUG-003
- API-09：创建题目 500 → BUG-002
- S-05/06/07（XSS 姓名/标题/学校名注入）：个人信息端点路径差异（`/api/user/profile`），本轮未完整执行注入-回显校验，需补测
- API-19/20（班级归档/恢复）：教师无班级列表端点（`GET /api/dean/classes` 教师返回 403），无法直接获取 class id 走查，建议补 registrar 角色账号测试

---

## 五、下一步建议

1. **优先修复 BUG-001/002/003**（同一 jsonb 根因 + dashboard 查询），修复后回归题库页与出题闭环。
2. 对齐测试方案文档与真实端点（题目 `stem` 字段、`/api/admin/*` 越权路径）。
3. 补充：XSS 三例注入回显校验、班级归档/恢复（registrar 账号）、全浏览器兼容性矩阵、打印导出、UI 交互细节（Toast/Loading/ESC）、性能 Lighthouse。
4. 阶段4 全流程（RG-01~04）建议人工浏览器走查确认。
