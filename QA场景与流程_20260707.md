# 知微AI教学助手 V2.4 — 用户场景 & 操作流程 & 测试覆盖

> **版本**: V2.4 | **日期**: 2026-07-07
> **方法论**: 场景驱动测试 (Scenario-Based Testing)

---

## 一、用户角色矩阵

```
            ┌──────────────────────────────────────────────┐
            │                   知微AI教学助手               │
            └──────────────────────────────────────────────┘
                                    │
        ┌───────┬───────┬───────┬───┴───┬───────┬───────┐
        │       │       │       │       │       │       │
    [教师]  [班主任] [校长] [管理员] [学生] [家长] [IT管理员]
      │       │       │       │       │       │       │
   本次测试  未测    测试    测试    Mock   Mock   部分测试
```

| 角色 | 测试账号 | 核心场景数 | 测试状态 |
|------|----------|:--------:|:--------:|
| 教师 | `13800000002 / teacher123` | 12 | 本次主测 |
| 班主任 | 同上(兼任) | 3 | 合并到教师 |
| 校长 | `13800000005 / teacher123` | 2 | 本次测 |
| 管理员 | `13800000001 / admin123` | 1 | 登录验证 |
| 学生 | Mock, 无独立登录 | 0 | 不测 |
| 家长 | Mock, 无独立登录 | 0 | 不测 |
| IT管理员 | 同上(管理员) | 0 | 不测 |

---

## 二、用户场景全景图

### S1: 教师工作日全流程

```
教师到校 → 打开首页 → 查看今日待办 → 备教案 → 出习题 → 组试卷
  → 布置作业 → 上课(小微辅助) → 课后批改 → 查看学情 → 关爱个别学生
  → 下班前签退
```

### S2: 教师学期初流程

```
登录 → 系统设置(确认班级/学科) → 训练小微(上传历史教案) → 开始备课
```

### S3: 教师学期末流程

```
登录 → 组期末试卷 → 批量出题 → 导出教案 → 归档无用班级 → 查看学期学情
```

### S4: 校长管理流程

```
登录 → 校长Dashboard → 查看全校统计 → 查看教师活跃度 → 审批
```

### S5: 数据维护流程

```
登录 → 系统设置 → 添加/编辑/归档学校班级 → 影响全站
```

---

## 三、操作流程 MIND（含分支）

```
知微 AI 教学助手 V2.4
│
├─ 1. 认证与个人信息
│   ├─ 登录 ──┬── 正确密码 → 跳转首页 ✅
│   │         └── 错误密码 → 提示"密码错误" ❌ → 返回登录页
│   ├─ 首页(TeacherDashboard)
│   │   ├── 统计卡片 ─── 教案/题目/待批改 数字正确 ✅
│   │   ├── 用户卡片
│   │   │   ├── 头像 ─── 点击修改 → 文件选择 → 预览 → 保存
│   │   │   ├── 催办 ─── 弹层 → 选择类型 → 发送 ──┬── 成功 → "已发送" ✅
│   │   │   │                                    └── 取消 → 关闭弹层
│   │   │   └── 数据 ─── 刷新后数字正确
│   │   ├── 7日/30日切换 ─── 点击切换Tab, 数据变化
│   │   ├── 新的创作 ─── 7个入口, 点击跳转正确
│   │   └── 近期草稿 ──┬── 有数据 → 表格显示
│   │                  └── 无数据 → "暂无草稿"
│   │
│   └─ 系统设置(帐号设置Tab)
│       ├── 头像 ─── 点击修改 → 文件选择 → 预览 → ✅/❌
│       ├── 用户ID ─── 复制 → "已复制"反馈 ✅/❌
│       ├── 名称 ─── 修改 → Enter保存 ──┬── 持久化(刷新不变) ✅
│       │                               └── 刷新丢失 ❌ ← P1 BUG
│       ├── 性别 ─── 修改 → Enter保存 → 刷新验证
│       ├── 手机号 ─── 修改 → Enter保存 → 刷新验证
│       ├── 邮箱 ─── 修改 → Enter保存 → 刷新验证
│       ├── 地区 ─── 修改 → Enter保存 → 刷新验证
│       └── 帐号移交 ─── 按钮存在(功能二期)
│
├─ 2. 教案(lesson-plans) ── 教学核心
│   ├─ 教案列表
│   │   ├── 显示 ──┬── 有数据 → 表格显示 4条
│   │   │         └── 无数据 → 空态提示
│   │   ├── 筛选
│   │   │   ├── 搜索 ─── 输入文字 → 列表过滤
│   │   │   ├── 状态 ─── all/draft/final → 过滤
│   │   │   └── 年级 ─── 下拉 → 过滤
│   │   ├── 操作(每行)
│   │   │   ├── 编辑 → 进入编辑页
│   │   │   ├── 预览 → 进入预览页
│   │   │   └── 删除 → ConfirmDialog → 确认 ──┬── 删除 ✅
│   │   │                                      └── 取消 → 关闭
│   │   └── 分页 ──┬── ≤8条 → 无分页
│   │              └── >8条 → 分页显示
│   │
│   ├─ 教案列表(已发布/published-lessons)
│   │   ├── 仅显示 status=final 的教案
│   │   ├── 操作 ─── 预览 / 复制 / 删除
│   │   └── 导出 ─── 点击 → 下载Word ✅/❌
│   │
│   └─ 教案编辑(/:id)
│       ├── 新建教案 (/new)
│       │   ├── 学科跟随 TeachingContext ✅
│       │   ├── 年级跟随 TeachingContext ✅
│       │   ├── AI生成 ── 点击"AI生成教案" → loading → 内容填充 ✅/❌
│       │   ├── 手动编辑 ── 改标题/目标/过程 → 实时保存
│       │   ├── 保存 ──┬── 草稿 → Toast ✅
│       │   │         └── 失败 → Toast错误 ❌
│       │   ├── 定稿 ──┬── 成功 → 状态→final
│       │   │         └── 无知识点 → 提示 ❌
│       │   └── 导出 ──┬── Word → 下载 ✅/❌
│       │              └── PDF → 下载 ✅/❌
│       │
│       └── 编辑已有教案
│           ├── 加载原有数据 → 回填 ✅
│           ├── 编辑 → 保存 → API调用 ✅
│           └── 定稿/导出 同上
│
├─ 3. 题库(exercises) ── 出题
│   ├─ 题目列表
│   │   ├── 显示 24题, content/type/difficulty 非空 ✅
│   │   ├── 筛选 ─── 搜索/类型/难度
│   │   ├── 操作 ─── 编辑/预览/删除
│   │   └── 分页
│   │
│   ├─ 出题生成器
│   │   ├── 学科/年级跟随TeachingContext ✅
│   │   ├── AI出题 ── 点击 → loading → 题目列表
│   │   ├── 语音输入 ── 点击麦克风 → 权限请求 → 语音转文字
│   │   ├── 合规检查 ── 生成后自动检查 → 不通过提示
│   │   ├── 保存到题库 ──┬── 成功 Toast ✅
│   │   │               └── 失败 Toast ❌
│   │   ├── 发布为作业 ── 选班级 → 填标题 → 发布 ✅
│   │   └── 导出 ── 打印/PDF ✅
│   │
│   └─ 题目编辑(/:id)
│       ├── 加载内容/答案/类型/难度 → 回填
│       ├── 编辑 → 保存 → PUT /exercises/:id ✅
│       ├── 存为模板 → 弹层 → 输入名称 → 保存
│       └── 预览模式(isPreview=1)
│
├─ 4. 试卷(exams) ── 组卷
│   ├─ 试卷列表
│   │   ├── 显示 3条(来自API) ✅
│   │   ├── 状态标签 ── draft/已发布
│   │   ├── 筛选 ─── 搜索/状态/类型
│   │   ├── 操作 ─── 编辑/预览/删除
│   │   └── 新建试卷 → 跳转 /exams/new
│   │
│   └─ 试卷编辑(/:id)
│       ├── 加载试卷信息
│       ├── 编辑标题 → 保存 → PUT /exams/:id ✅
│       ├── 存为模板 → 弹层
│       └── 打印试卷 → 打印 ✅/❌
│
├─ 5. 作业(assignments) ── 布置批改
│   ├─ 作业列表
│   │   ├── 显示作业列表 ✅
│   │   ├── 状态 ─── published/scheduled/draft
│   │   ├── 截止日期显示
│   │   └── 操作 ─── 编辑/预览/删除
│   │
│   └─ 作业编辑
│       ├── 新建作业 → 选题目 → 设置截止日期 → 发布
│       ├── 保存 → 草稿 Toast ✅
│       └── 发布 → Toast ✅
│
├─ 6. 素材(materials) ── 教辅资源
│   ├─ 素材列表
│   │   ├── 显示 8条素材 ✅
│   │   ├── 视图切换 ─── 网格/列表
│   │   ├── 筛选 ─── 图片/文档/音频/视频
│   │   ├── 分组 ─── 下拉筛选
│   │   └── 空数据态 → 空提示
│   │
│   ├─ 上传素材
│   │   ├── 点"上传素材" → 文件选择
│   │   ├── 选文件 → 上传 → Toast反馈 → 列表刷新 ✅
│   │   ├── 超大文件(>50MB) → 错误提示 ✅/❌
│   │   └── 多文件同时上传 → 逐条反馈
│   │
│   └─ 教辅频道
│       └── 点"教辅频道" → 功能预告弹窗 ✅
│
├─ 7. 教案互审(review-pool)
│   ├─ 互审列表
│   │   ├── 按当前学科自动过滤
│   │   ├── 按单元筛选
│   │   ├── 操作 ─── 审阅/通过/退回/留言
│   │   ├── 删除 → ConfirmDialog → 确认 → Toast反馈 ✅
│   │   └── 归档班级互审项 → 自动移除提示
│   │
│   └─ 互审详情(页面暂未实现, 仅为行操作)
│
├─ 8. 学情分析(analytics) ── 数据看板
│   ├─ 概览数据
│   │   ├── avg_score → 来自API ✅/❌(无exam_scores数据默认为65)
│   │   ├── completion_rate → 来自API ✅
│   │   └── mastery/total_students → Mock数据 ⚠️
│   ├─ 知识图谱(Mock) ⚠️
│   ├─ 学生列表(Mock) ⚠️
│   └─ 时间切换 ─── 本周/本月/本学期
│
├─ 9. 成长关爱(care) ── 学生关怀
│   ├─ 关怀列表(CarePage)
│   │   ├── 显示 7名学生 ✅
│   │   ├── 进度条/正确率/趋势 ✅
│   │   ├── 添加关怀 → 弹层表单 → 新增 ✅
│   │   ├── 编辑关注点 → 行内🖊 → Enter保存 ✅
│   │   ├── 点击学生 → 进入详情
│   │   └── 归档班级提示 ⚠️
│   │
│   └─ 关怀详情(CareDetail)
│       ├── 学业概览 → 正确率/趋势图 ✅
│       ├── 本周聚焦 → 编辑 → ✓保存 → Toast ✅
│       ├── 教师备注 → 编辑 → ✓保存 → Toast ✅
│       ├── 任务列表 → 勾选/取消
│       ├── 评估日志 → 添加评估 → 填文本 → 添加 ✅
│       ├── 时间线 → 显示
│       └── 留言 → 输入 → 发送
│
├─ 10. 系统设置(settings) ── 配置管理
│   ├─ 帐号设置
│   │   ├── 头像 ── 修改 ✅
│   │   ├── 用户ID ── 复制 ✅
│   │   ├── 名称/性别/手机/邮箱/地区 ── 行内编辑 ✅/❌(持久化)
│   │   └── 帐号移交 ── 按钮存在
│   │
│   ├─ 学校·班级
│   │   ├── 添加学校 → 弹窗①学校信息 → ②班级配置
│   │   │   ├── 保存学校 → 确认弹层 → 步骤2激活
│   │   │   ├── 添加班级 → 填年级/班名/学科 → 去重检查
│   │   │   └── 取消 → 关闭弹窗
│   │   ├── 编辑学校 → 弹窗① → 预填 → 保存 → 确认
│   │   ├── 编辑班级 → 弹窗② → 学科切换 → 保存 → 确认 ✅
│   │   ├── 归档学校 → 确认弹层(红色) → 显示班级数 → 确认归档
│   │   ├── 归档班级 → 确认弹层 → 显示影响 → 确认归档
│   │   └── 添加学校/班级/编辑 错误输入处理
│   │       ├── 空名称 → 不保存 ✅/❌
│   │       ├── 重复班名 → "已存在"提示 ✅
│   │       └── 学科为空 → 不允许 ✅/❌
│   │
│   ├─ 训练小微
│   │   ├── 上传教案 → 显示文件名 ✅
│   │   ├── 贴评语 → 文本输入
│   │   ├── 贴反思 → 文本输入
│   │   └── 风格摘要 → 显示
│   │
│   └─ 日志·反馈
│       ├── 上传日志 → 文件选择 → "已上传✓" ✅
│       └── 意见反馈 → 弹层 → 填表 → 提交 → "已提交✓" ✅
│
├─ 11. 通知系统
│   ├─ 铃铛下拉
│   │   ├── 显示5条通知 ✅
│   │   ├── 红点计数(未读数) ✅
│   │   ├── 全部已读 → 红点消失 ✅
│   │   ├── 点击通知 → 跳转(部分无跳转)
│   │   └── 空通知态(全部已读后)
│   │
│   └─ Toast全局
│       ├── 成功(green) → 2s消失 ✅
│       ├── 失败(red) → 3s消失 ✅
│       ├── 警告(yellow) ✅
│       └── 20处alert全部替换 ✅
│
├─ 12. 班级切换
│   ├── 打开弹层 → 选学校 → 选班级
│   ├── 切换 → TeachingContext更新
│   ├── 切换后首页/教案/题库跟随变化
│   └── 当前班级标记 ✓
│
├─ 13. 校长Dashboard(principal)
│   ├── 登录(校长账号) → 返回role=principal
│   ├── Dashboard → 教师数/学生数/班级数/教案数 ✅
│   └── Analytics → 年级成绩(空数据)
│
├─ 14. 小微AI助手
│   ├── 桌面浮动按钮(右下角)
│   │   ├── 点击打开 → 对话面板 ✅
│   │   ├── 最小化 → 缩略小条 → 点击恢复 ✅
│   │   └── 关闭 → 浮动按钮 ✅
│   ├── 功能推荐卡片 → 点击跳转
│   ├── 快速指令 → 点击发送
│   ├── 文本对话 → 输入 → 发送 → AI回复
│   ├── 语音输入 → 录音 → 转文字 → 发送
│   ├── 图片输入 → 拍照/选图 → 发送
│   └── 对话历史 → 保持(刷新后消失)
│
├─ 15. Token余额(HeaderRight)
│   ├── Token显示(5%) → 静态, 15分钟刷新一次
│   └── 图标不旋转(已修复)
│
└─ 16. 跨场景持久化验证
    ├─ 页面刷新 → 数据不丢失(localStorage/API)
    ├─ 浏览器后退 → 状态保持
    ├─ 多Tab同时打开 → 数据一致
    └─ 网络断开 → 错误提示
```

---

## 四、操作流程 MIND 图（精简示意版）

```
教师登录 ──────→ 首页Dashboard
                    │
        ┌───────────┼───────────┐
        │           │           │
    系统设置      教案管理      题库出题
        │           │           │
    ┌───┴───┐   ┌──┴──┐    ┌──┴──┐
   帐号 学校班级 列表 编辑  列表 出题器
    │    │       │   │     │    │
   编辑 添加     │  保存   │   AI生成
   (持久化?) │   │  定稿   │   发布作业
        归档     │  导出   编辑
             预览/删除   保存
                             存模板

试卷组卷 ──→ 作业布置 ──→ 批阅(未实现)
    │            │
  列表/编辑   列表/编辑

素材上传 ──→ 素材浏览
    │
  上传(FormData)

学情分析 ──→ 师生数据
    │
   API聚合

成长关爱 ──→ 关怀详情
    │
  添加/编辑/评估

教案互审 ──→ 行操作(通过/退回/删除)

通知 ──→ 铃铛/Toast

班级切换 ──→ TeachingContext
    │
  全局影响
```

---

## 五、测试分支清单

基于以上场景，分解为可执行的测试分支：

### A. 持久化链路 (🔴 高优先级 — 本次盲区)

| # | 测试项 | 操作 | 验证方法 | 预期 |
|---|--------|------|---------|------|
| A1 | 名称持久化 | 系统设置改名称 → 保存 → 刷新 | 刷新后名称不变 | ❓ |
| A2 | 手机号持久化 | 同上 | 同上 | ❓ |
| A3 | 邮箱持久化 | 同上 | 同上 | ❓ |
| A4 | 性别持久化 | 同上 | 同上 | ❓ |
| A5 | 地区持久化 | 同上 | 同上 | ❓ |
| A6 | 头像持久化 | 改头像 → 刷新 | 头像不变 | ❓ |
| A7 | 教案保存持久化 | 编辑教案 → 保存 → 回列表 → 再进入 | 内容不变 | ❓ |
| A8 | 题目保存持久化 | 编辑题目 → 保存 → 回列表 | 内容更新 | ❓ |
| A9 | 试卷保存持久化 | 改试卷标题 → 保存 → 回列表 | 标题更新 | ❓ |
| A10 | 班级编辑持久化 | 编辑班级 → 保存 → 关弹窗 → 重开 | 学科变更生效 | ❓ |
| A11 | 学校编辑持久化 | 编辑学校 → 保存 → 关弹窗 → 重开 | 名称变更生效 | ❓ |
| A12 | 关怀编辑持久化 | 编辑关注点/备注 → 返回列表 → 重进详情 | 内容不变 | ❓ |

### B. 交互闭环 (🟡 中优先级)

| # | 测试项 | 操作 | 验证方法 | 预期 |
|---|--------|------|---------|------|
| B1 | 催办完整流程 | 点催办 → 选类型 → 发送 | 显示"已发送" | ✅已验证 |
| B2 | 通知已读完整流程 | 开铃铛 → 全部已读 → 关 → 重开 | 红点消失 | ✅已验证 |
| B3 | 素材上传→列表刷新 | 上传文件 → 关弹窗 → 列表增加 | 新文件在列表中 | ❓ |
| B4 | 学校归档→列表消失 | 归档学校 → 关弹窗 | 学校不在列表 | ❓ |
| B5 | 班级归档→列表消失 | 归档班级 → 关弹窗 | 班级不在列表 | ❓ |
| B6 | 添加关怀→列表刷新 | 添加学生 → 关弹窗 | 新学生在列表 | ❓ |
| B7 | 关怀详情编辑→列表反馈 | 在详情改关注点 → 回列表 | 列表关注点文字更新 | ❓ |

### C. 空数据/边界 (🟢 低优先级)

| # | 测试项 | 操作 | 验证方法 | 预期 |
|---|--------|------|---------|------|
| C1 | 空教案列表 | 清空教案 → 列表 | "暂无教案" | ❓ |
| C2 | 空试卷列表 | 清空试卷 → 列表 | "暂无试卷" | ❓ |
| C3 | 空关怀列表 | 删完关怀 → 列表 | 空态提示 | ❓ |
| C4 | 学科为空编辑 | 编辑学科为空 → 保存 | 禁止保存/提示 | ❓ |
| C5 | 超长名称 | 输入超长学校名 | 不崩溃 | ❓ |

### D. 多角色 (🔵 低优先级)

| # | 测试项 | 角色 | 验证方法 |
|---|--------|------|---------|
| D1 | 校长Dashboard | 13800000005 | 教师/学生/班级数正确 |
| D2 | 管理员登录 | 13800000001 | role=it_admin, 有限权限 |
| D3 | 教师不能看校长Dashboard | 13800000002 | 403 Forbidden |

### E. 断点续传 (🟡 中优先级 — 本次补充)

| # | 测试项 | 操作 | 验证方法 | 预期 | 现状态 |
|---|--------|------|---------|------|:----:|
| E1 | 素材上传中断恢复 | 选大文件 → 上传中切飞行模式 → 恢复网络 | 能否续传/重新开始 | 续传或明确提示 | 🔴 未实现 |
| E2 | 教案编辑自动保存 | 编辑教案 → 不点保存直接关浏览器 → 重开同一教案 | localStorage/草稿恢复 | 内容在草稿中 | ❓ |
| E3 | 教务编辑突然关闭 | 在设置页编辑班级 → 不点保存直接刷新 → 重新打开编辑面板 | 未保存内容丢失 | 提示"未保存"或已自动暂存 | ❓ |
| E4 | 小微训练断点 | 训练流程(教案→评语→反思) → 完成两步后关窗口 → 重新打开 | 能从第三步继续 | 进度保持 | ❓ |
| E5 | 对话历史保持 | 小微对话若干条 → 关闭面板 → 重新打开 | 历史存在 | 历史保留 | ❓ |
| E6 | 表单输入不丢失 | 在弹窗(添加学校/班级/关怀)中填了一半 → 误点遮罩关闭 → 重新打开 | 已填内容还在 | 内容保留或明确警告 | ❓ |

### F. 特殊字符/安全注入 (🔴 高优先级 — 本次补充)

| # | 测试项 | 输入内容 | 验证方法 | 预期 | 风险 |
|---|--------|---------|---------|------|:--:|
| F1 | XSS—名称注入 | 用户名称改为 `<script>alert(1)</script>` | 保存后再打开页面 | 不弹窗，显示为纯文本 | 🔴 |
| F2 | XSS—教案标题注入 | 教案标题含 `<img src=x onerror=alert(1)>` | 保存→列表页查看 | 不执行 | 🔴 |
| F3 | XSS—学校名称注入 | 学校名称含 `<a href=javascript:alert(1)>click</a>` | 保存后查看 | 不执行 | 🔴 |
| F4 | SQL注入—搜索框 | 搜索框输入 `'; DROP TABLE users;--` | 执行搜索 | 不报错/空结果 | 🔴 |
| F5 | SQL注入—登录框 | 手机号输入 `' OR '1'='1` | 尝试登录 | 拒绝登录 | 🔴 |
| F6 | 生僻字—学生姓名 | 名称改为 `张𠀋` | 保存→显示 | 不出现方框或? | 🟡 |
| F7 | 生僻字—学校名称 | 学校名称含生僻字 | 保存→显示 | 正确显示 | 🟡 |
| F8 | 数学符号—题目内容 | 题目内容含 `∫₀²π sin(x)dx` | 保存→再打开 | 符号不变成乱码 | 🟡 |
| F9 | 数学符号—试卷标题 | 标题含 `√9 × ∑₁³` | 保存→列表显示 | 不报错不截断 | 🟡 |
| F10 | 英文引号—教案内容 | 教案正文含 `He said "hello"` | 保存→再打开 | 引号不变形 | 🟡 |
| F11 | 中文引号—全角半角 | 标题含 `"观潮"中的"天下奇观"` | 保存→搜索该标题 | 搜索/显示正常 | 🟡 |
| F12 | 换行符—题目内容 | 题目选项含 `\n` | 保存→再打开 | 换行保留 | 🟢 |
| F13 | Emoji—教师名称 | 名称改为 `张老师😊` | 保存→显示 | DB编码支持/过滤 | 🟢 |
| F14 | Emoji—教案内容 | 教案正文含 `👍✅` | 保存→打开 | 报错或正确显示 | 🟢 |
| F15 | 超长输入—学校全称 | 填写200个汉字的学校名称 | 保存 | 不崩溃 | 🟢 |
| F16 | 纯空格—班名 | 班名输入全空格 | 保存 | 拒绝或trim | 🟢 |

### G. 兼容性 (🟡 中优先级 — 本次补充)

| # | 测试项 | 环境/条件 | 验证方法 | 预期 | 风险 |
|---|--------|---------|---------|------|:--:|
| G1 | Chrome 兼容 | Chrome 最新版 | 全功能走一遍 | 无异常 | 🟡 |
| G2 | Safari 兼容 | Safari 17+ (macOS/iPad) | 登录→教案→出题→设置 | 无异常，尤其文件上传 | 🟡 |
| G3 | Firefox 兼容 | Firefox 最新版 | 核心流程 | 无异常 | 🟢 |
| G4 | Edge 兼容 | Edge 最新版 | 核心流程 | 无异常 | 🟢 |
| G5 | 分辨率—1366×768 | 模拟笔记本分辨率 | 侧边栏/表格/弹窗无遮挡 | 不出现横向滚动 | 🟡 |
| G6 | 分辨率—1920×1080 | 标准桌面 | 布局正常 | 充分利用宽度 | 🟢 |
| G7 | 分辨率—1024×768 | 模拟小屏/平板 | 内容适配 | 不溢出，必要时自适应 | 🟡 |
| G8 | 移动端响应式 | iPhone/iPad Safari | 教师端页面缩放 | WAP端正常(已有WAP) | 🟢 |
| G9 | 慢网速—3G模拟 | Chrome DevTools Slow 3G | 登录→首页→教案列表 | Loading状态显示，不白屏 | 🟡 |
| G10 | 网络断开 | 操作中断开WiFi | 任何操作 | 错误提示或离线提示 | 🟡 |
| G11 | 字体兼容 | macOS/Windows 默认中文字体 | 生僻字/数学符号渲染 | 不出现方框 | 🟡 |
| G12 | 打印兼容 | Chrome打印教案/试卷 | 打印预览 | 排版不溢出 | 🟢 |

### H. 安全与性能 (🔴 高优先级 — 本次补充)

| # | 测试项 | 操作 | 验证方法 | 预期 | 风险 |
|---|--------|------|---------|------|:--:|
| H1 | Token过期 | 登录后等15分钟 → 操作 | Token刷新机制 | 自动刷新或跳转登录 | 🔴 |
| H2 | 未登录直接访问URL | 直接访问 `/lesson-plans` | 跳转登录 | 重定向到 `/login` | 🔴 |
| H3 | 越权—教师访问校长API | 用教师Token调 `/api/principal/dashboard` | curl测试 | 403 Forbidden | 🔴 |
| H4 | 密码弱口令 | 注册/修改密码输 `123456` | 拒绝或提示 | 要求8位+混合 | 🟡 |
| H5 | 密码明文传输 | 抓包看登录请求 | Wireshark/DevTools | 密码不在URL/明文body(应走HTTPS) | 🔴 |
| H6 | 并发操作—同一教案编辑 | 两个Tab同时编辑同一教案 | 先保存→后保存 | 后者覆盖或冲突提示 | 🟡 |
| H7 | 大列表性能—100条教案 | 列表加载100+条 | 滚动流畅 | 不卡顿(虚拟滚动或分页) | 🟡 |
| H8 | API响应时间 | 逐个API计时 | curl -w计时 | <500ms(P95) | 🟡 |
| H9 | 内存占用—长时间运行 | 使用30分钟后 | Chrome Task Manager | 不持续增长(泄漏) | 🟡 |
| H10 | 复制粘贴—富文本 | 从Word粘贴到教案编辑器 | 格式 | 保留基本格式或纯文本 | 🟢 |
| H11 | 证书/HTTPS | 部署后访问 school.ziwi.cn | 浏览器地址栏 | 显示🔒 | 🔴 |

---

## 六、统计

| 类别 | 测试项数 | 待测试 |
|------|:------:|:------:|
| A 持久化链路 | 12 | 12 |
| B 交互闭环 | 7 | 5 |
| C 边界/异常 | 5 | 5 |
| D 多角色 | 3 | 3 |
| E 断点续传 | 6 | 6 |
| F 特殊字符/安全注入 | 16 | 16 |
| G 兼容性 | 12 | 12 |
| H 安全与性能 | 11 | 11 |
| **合计** | **72** | **70** |

---

> **下一步**: 逐项执行上述72个测试分支，记录Bug，形成Bug List。
> 请确认场景和流程是否覆盖全面，再开始执行。
