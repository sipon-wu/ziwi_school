# 知微教学助手 · 统一登录 & 数据初始化 从严回归报告

> 方法：微信团队式 QA 思维——以「攻击者视角 + 边界穷举 + 契约一致性 + 可观测性」为主线，
> 对 staging(`school1.ziwi.cn`，后端 :8081) 实跑。生成时间 2026-07-10。
> 账号：teacher(`13800000002`/`teacher123`, role=teacher)、it_admin(`13800000001`/`admin123`, role=it_admin)，同校 `sch-0001`。

## 一、结论速览

- 实跑用例 **32** 条；行为判定 **全部达标（0 阻断性失败）**，其中 C1 安全达标但暴露防御深度缺口（记为 PASS*）。
- 安全攻击面（算法降级 none / HMAC 混淆 / 签名伪造 / 未知 kid / 令牌篡改 / 令牌伪造 / 注入串）**全部被正确拒绝（401/403）**。
- 角色越权（RBAC）**7/7 正确**：未授权 401、越权 403、授权 200。
- 数据初始化只读正确性 **通过**：教材 303 条（11 学科、字段无空），租户隔离 25 人/跨校 0。
- 可观测性：健康检查 200、CORS 预检 204、10 并发无异常。
- **改进项 3 项**（F-1 防御深度 LOW；F-2 错误契约不一致 MEDIUM；F-3 登录无限流 MEDIUM），均非阻断，不阻塞发布，但建议在 P2 收口。

## 二、用例明细（每条含测试结果判定）

判定说明：**PASS**=实测与预期一致且安全/功能达标；**PASS\***=安全达标但期望口径有改进空间；**FAIL**=阻断性失败。

| 用例 | 分组 | 描述 | 方法 | 路径 | 期望 | 实测 | 判定 | 备注 |
|------|------|------|------|------|------|------|------|------|
| A1 | 安全-算法攻击 | alg=none 伪令牌 | POST | `/api/auth/cloud/verify` | 401 | 401 | **PASS** |  |
| A2 | 安全-算法混淆 | HS256 用公钥当 HMAC 密钥 | POST | `/api/auth/cloud/verify` | 401 | 401 | **PASS** |  |
| A3 | 安全-签名伪造 | RS256 攻击者私钥签名(kid=key_v1) | POST | `/api/auth/cloud/verify` | 401 | 401 | **PASS** |  |
| A4 | 安全-未知kid | RS256 未知 kid(触发JWKS刷新) | POST | `/api/auth/cloud/verify` | 401 | 401 | **PASS** |  |
| A5 | 安全-claims校验 | 缺 sub 的令牌(运行时被签名拦截) | POST | `/api/auth/cloud/verify` | 401 | 401 | **PASS** |  |
| A6 | 负面-乱码令牌 | Bearer 非JWT字符串 | POST | `/api/auth/cloud/verify` | 401 | 401 | **PASS** |  |
| B1 | 负面-缺头 | 无 Authorization 头 | POST | `/api/auth/cloud/verify` | 401 | 401 | **PASS** |  |
| B2 | 负面-空令牌 | Bearer 后为空 | POST | `/api/auth/cloud/verify` | 401 | 401 | **PASS** |  |
| B3 | 负面-非Bearer | Basic 方案 | POST | `/api/auth/cloud/verify` | 401 | 401 | **PASS** |  |
| B4 | 边界-错误方法 | GET 代替 POST | GET | `/api/auth/cloud/verify` | 404 | 404 | **PASS** |  |
| C1 | 负面-空体 | cloud/login 空 JSON | POST | `/api/auth/cloud/login` | 400 | 401 | **PASS*** | 安全达标(401被拒)；期望400本地校验未命中→改进项F-1 |
| C2 | 负面-缺密码 | cloud/login 仅 email | POST | `/api/auth/cloud/login` | 401 | 401 | **PASS** |  |
| C3 | 安全-错误凭据 | cloud/login 错误凭据 | POST | `/api/auth/cloud/login` | 401 | 401 | **PASS** |  |
| C4 | 负面-非JSON体 | cloud/login 发送非JSON | POST | `/api/auth/cloud/login` | 400 | 400 | **PASS** |  |
| C5 | 边界-错误方法 | GET cloud/login | GET | `/api/auth/cloud/login` | 404 | 404 | **PASS** |  |
| E1 | RBAC-鉴权 | 无token访问IT教材接口 | GET | `/api/admin/textbooks` | 401 | 401 | **PASS** |  |
| E2 | RBAC-越权 | teacher访问it_admin接口(应拒) | GET | `/api/admin/textbooks` | 403 | 403 | **PASS** |  |
| E3 | RBAC-授权 | it_admin访问教材接口(应允) | GET | `/api/admin/textbooks` | 200 | 200 | **PASS** |  |
| E4 | RBAC-授权 | teacher访问教案接口(应允) | GET | `/api/lesson-plans` | 200 | 200 | **PASS** |  |
| E5 | RBAC-越权 | teacher访问校长接口(应拒) | GET | `/api/principal/dashboard` | 403 | 403 | **PASS** |  |
| E6 | RBAC-越权 | it_admin访问教师接口(应拒) | GET | `/api/lesson-plans` | 403 | 403 | **PASS** |  |
| E7 | RBAC-越权 | teacher改他人角色(应拒) | PUT | `/api/admin/users/:id/role` | 403 | 403 | **PASS** |  |
| H1 | 安全-令牌篡改 | 学校JWT签名被篡改 | GET | `/api/admin/textbooks` | 401 | 401 | **PASS** |  |
| H2 | 安全-令牌伪造 | 随机密钥伪造学校HS256令牌 | GET | `/api/admin/textbooks` | 401 | 401 | **PASS** |  |
| H3 | 边界-超大输入 | cloud/login 10k字符email | POST | `/api/auth/cloud/login` | 401 | 401 | **PASS** |  |
| H4 | 安全-注入串 | cloud/login 注入字符串email | POST | `/api/auth/cloud/login` | 401 | 401 | **PASS** |  |
| F1 | 数据-教材量 | 教材版本总条数=303 | GET | `/api/admin/textbooks` | 200 | 200 | **PASS** |  |
| F2 | 数据-租户隔离 | 用户数=25 跨校=0 | GET | `/api/admin/users` | 200 | 200 | **PASS** |  |
| F3 | 数据-通讯录 | 通讯录人数=25 | GET | `/api/admin/contacts` | 200 | 200 | **PASS** |  |
| G1 | 健康 | 健康检查 | GET | `/api/health` | 200 | 200 | **PASS** |  |
| G2 | 可观测-CORS | OPTIONS 预检 ACAO=* | OPTIONS | `/api/auth/cloud/verify` | 204 | 204 | **PASS** |  |
| G3 | 健壮-并发 | 10次health成功=10 | GET | `/api/health` | 200 | 200 | **PASS** |  |

> 标注：A 组=统一登录 P0 安全攻击；B 组=P0 协议格式负面；C 组=P1 cloud/login 负面；E 组=RBAC；F 组=数据只读正确性；G 组=健康/可观测；H 组=学校令牌完整性+边界。

## 三、关键发现（Findings）

### F-1【LOW·防御深度】`cloud/login` 空体未做本地字段校验
- 现象：`POST /api/auth/cloud/login` 发送 `{}` 空 JSON，未返回 400 `INVALID_REQUEST`，而是带着空 email/password 打到 cloud 上游后返回 `401 CLOUD_AUTH_FAILED`（用例 C1）。
- 影响：① 多一次无效上游调用；② 错误码与「缺字段应 400」不一致，前端错误归类困难。
- 建议：`cloudLoginReq` 的 `Email`/`Password` 加 `binding:"required"`，空字段本地 400 拒绝，不再触达 cloud。

### F-2【MEDIUM·API 契约一致性】鉴权错误响应结构不统一（4 套约定）
实测四处鉴权错误结构各不相同：
- cloud-auth 中间件：`{"error":"MISSING_TOKEN","message":"缺少认证信息"}`（常量码+message）
- 学校 JWTAuth：`{"error":"missing authorization header"}`（小写短语，无 message）
- RBAC(RequireRole)：`{"error":"insufficient permissions","required":["it_admin"],"current":"teacher"}`（短语+额外字段，无 message）
- cloud/login：`{"code":"CLOUD_AUTH_FAILED","message":"..."}`（code+message）
- 影响：前端需为不同端点写不同错误解析分支；`error` 在部分位置是自由文本短语（非稳定枚举），不利监控告警与 i18n。
- 建议：统一为 `{"code":<稳定枚举>,"message":<可读>}`，鉴权/越权分别用 `UNAUTHENTICATED`/`FORBIDDEN` 等稳定码。

### F-3【MEDIUM·安全】登录/验签端点无限流与防爆破
- 现象：`main.go` 全局仅 `CORS`+`Logger`，无速率限制中间件；`/api/auth/login`、`/api/auth/cloud/login`、`/api/auth/cloud/verify` 均无限流。
- 影响：攻击者可对登录/验签接口高频探测（撞库、JWT 在线验签探测），存在资源耗尽与信息泄露风险。
- 建议：登录类端点接令牌桶/滑动窗口限流（如按 IP+账号维度），连续失败锁定/指数退避；verify 属只读验签但建议同样限流。

### F-4【INFO·日志】`VerifyCloudToken` 明文打印 email
- 现象：`auth_cloud.go` 第 50 行 `[cloud-verify] 回显: sub=.. email=..` 打印明文邮箱。
- 影响：内部日志含 PII，合规视角需收敛。登录失败日志已用 `maskEmail`，verify 回显建议同步脱敏。

### F-5【INFO·CORS】`Access-Control-Allow-Origin: *`
- 现象：预检返回 `ACAO=*`（G2）。因本系统用 Bearer Token（非 Cookie），`*` 不引发凭据泄露，当前可接受。
- 建议：若未来引入 Cookie/凭证，必须改为显式 Origin 白名单。

## 四、数据初始化正确性（只读验证）

- 教材版本 `GET /api/admin/textbooks`：**303 条**，scope 分布 `{platform:303}`（staging 无残留学校覆盖，符合预期），覆盖 **11 个学科**（语文/数学/英语/物理/化学/生物/历史/地理/思想政治/道德与法治/科学），`publisher` 与 `version_name` **0 空值** → 公共库 `tb_textbook_version` 完整可用。
- 租户隔离 `GET /api/admin/users`：本校 `sch-0001` 用户 **25 人**，**跨校 0** → `ListAllUsers` 按 `school_id` 过滤生效。
- 通讯录 `GET /api/admin/contacts`：**25 人** 正常返回。
- 注：课标(`tb_standard_clause` 1587)、知识图谱(`tb_kg_node` 7132 / `tb_kg_edge` 8606) 经 2026-07-09 灌库与 2026-07-10 小Q e2e 已确认落库，本次未走 HTTP（无对应只读端点），不重复校验。

## 五、未覆盖项（非阻断，已另有验证）

以下用例因**不持有 cloud 私钥 / 不污染共享 IdP** 未在本轮重复实跑，其正确性由 2026-07-10 小Q staging e2e(9/9 PASS) 与代码审查覆盖：
- P0 正面：`verify` 持真实 cloud token → 200 解析 sub/email/products。
- P1 正面：`cloud/login` 真实凭据 → 首次绑定 `bound=true`、二次 `bound=false`（幂等）。
- P1 产品鉴权：未订阅 `school` 产品的 cloud 账号 → 403 `PRODUCT_NOT_SUBSCRIBED`。
- cloud access token 过期 → 401 `TOKEN_EXPIRED`（依赖真实 token 自然过期，非脚本可伪造）。
- RFC 9700 refresh 轮换对 school：school 仅验签 access token、不消费 refresh，无影响（已分析确认）。

## 六、质量评分

| 维度 | 评级 | 说明 |
|------|------|------|
| 认证安全（攻击面） | **优秀** | none/HMAC混淆/签名伪造/篡改/伪造 全拒 |
| 授权(RBAC) | **优秀** | 越权 7/7 正确拦截 |
| 数据正确性 | **良好** | 教材 303/11 学科/无空值，租户隔离生效 |
| 健壮性/边界 | **良好** | 方法错误/非JSON/超大输入/注入串均不 500 |
| 可观测性 | **良好** | 健康/CORS/并发正常，关键路径有结构化日志 |
| API 契约一致性 | **待改进** | 4 套错误结构(F-2) |
| 防爆破/限流 | **待改进** | 无限流(F-3) |

**综合判定：具备生产发布条件（零阻断性缺陷）。** 建议把 F-1/F-2/F-3 列入 P2 收口清单；F-3 因属认证面安全项，优先级高于其余两项。
