# QA 测试报告 · 课件装饰导出与 pptx 投屏件

- **日期**：2026-09-18
- **环境**：staging `http://school1.ziwi.cn`（部署目标 `/var/www/school1.ziwi.cn`）
- **部署方式**：`qa/build_deploy.sh`（仅前端构建 + rsync；后端未改动，未重建容器）
- **部署来源**：本地工作区**未提交**代码（改动随构建进 staging，尚未 git commit，见文末建议）
- **执行人**：自动化（用户离线，睡前委托）

---

## 一、本轮改动清单（本次部署内容）

| 文件 | 改动 | 性质 |
|---|---|---|
| `code/frontend/src/lib/exportPptx.ts` | ① 装饰层导出：封面/内容两分支接入 `drawDecor`（背景衬底 + 页眉/页脚/四角/浮动） | 功能 |
| 同 | ② 背景取消 canvas 预模糊，改 `transparency:82`（≈0.18）平铺，**无模糊**，对齐 commit `708df77`（屏幕端已无模糊，为导出保真让路） | 一致性 |
| 同 | ③ `buildCoursewareSlides`（markdown 字符串路径）合成封面时解析 `CW-COVER`，消除潜在不一致 | 一致性 |
| 同 | ④ **关键修复**：`toPptxImage` 由 `createImageBitmap` 改为 `new Image()` 光栅化（见第三节 bug） | **Bug 修复** |
| `code/frontend/src/pages/Materials.tsx` | 素材库 PPT 预览带入 `coverDecor`（`parseCoverDecor(content)`），修复浏览课件时封面装饰丢失 | Bug 修复 |

---

## 二、e2e 测试结果

### 套件 A：回归 `qa/regression_20260917.cjs`（覆盖本轮全部改动面）
- **断言 37 条，失败 0 条 ✔**
- 覆盖：环境健康 / KG 默认页与单元下拉 / H5 封面与页码与衬底 / PPT 编辑器页数口径 / 封面素材「占位→换→保存→持久化」全流程 / 全程 pageerror=0 / 发布流程 / 内容页装饰随 markdown 往返（CW-DECOR）
- 日志：`/tmp/e2e_20260918.log`

### 套件 B：pptx 装饰导出专项 `qa/verify_pptx_decor_export.cjs`（新增，针对本次核心改动）
- **断言 6 条，失败 0 条 ✔**
- 流程：注入封面装饰（背景 + 角标）→ 在线编辑器打开 → 真实点击「一键导出」→ 捕获 `.pptx` 下载 → 解包核验
- **关键证据**：`ppt/media/` 内含 **2 张真实图片** `image-1-1.png`、`image-1-2.png`（背景衬底 + 角标均已嵌入导出件），证明装饰真正进入 pptx
- 日志：`/tmp/e2e_pptx_decor_20260918.log`

---

## 三、过程中发现并修正的问题（偏差）

### ①【严重 · 真 bug，已修复】装饰根本没进 pptx
- **现象**：首版 `toPptxImage` 用 `createImageBitmap(blob)` 光栅化 data-url SVG，在本环境 Chromium 抛 `InvalidStateError: The source image could not be decoded`，被 `catch` 静默吞掉 → `imgMap` 恒为空 → 导出件 `ppt/media/` **空目录**、装饰全丢。
- **暴露方式**：专项 e2e 解包 `ppt/media/` 发现只有目录项、无图片文件（初版断言把目录项误数成"图片"导致**假绿**，已修正）。
- **修复**：改用 `new Image()` + `drawImage` 光栅化（隔离探测已确认 `Image` 路径在 data-url SVG/PNG 下正常，`createImageBitmap` 失败）。复测后 `ppt/media/` 含 2 张真实图片，bug 闭环。

### ②【测试陈旧 · 已修正】回归脚本 2 条断言与代码冲突
- 脚本第 106、155 行仍断言封面衬底"**带高斯模糊**"，但 commit `708df77` 已把屏幕端（PPT 编辑器 + H5）衬底改为"仅 0.18 半透明、无模糊"（"为导出保真让路"）。源码 `PptxPreview.tsx:262`、`renderer.ts:473` 均确认无 blur。
- 已将该两条断言修正为"**无高斯模糊**"，使 e2e 真实校验当前行为（属测试维护，非掩盖）。

### ③【测试质量 · 已修正】unzip 目录项假绿
- 初版 `countMedia` 用 `/ppt\/media\//` 过滤 `unzip -l` 输出，会把目录项 `ppt/media/` 计为"1 张图片"造成假绿。已改为同时返回文件名列表并断言真实图片文件存在。

---

## 四、遗留与建议

1. **本次改动尚未 git commit**（部署来自工作区未提交代码）。建议明天复核后提交：`exportPptx.ts` + `Materials.tsx` + 两个 e2e 脚本 + 回归脚本断言修正。
2. **G6 知识图谱 `Maximum update depth` 预存缺陷**（记忆 66942359）不在本轮范围，应单独立项。
3. 建议将 `regression_20260917.cjs` 与新增 `verify_pptx_decor_export.cjs` 固化进 CI / 预提交门禁（`qa/lib/assert.cjs` 已具备 report 与分母断言能力）。
4. 本次 e2e 仅覆盖装饰导出与往返；若需完整验收，建议补一条"真实浏览器导出 pptx 并人工目检视觉与屏幕一致"的人工关卡（自动化解包只能确认"图片存在"，不能确认像素级观感）。

---

## 五、结论

- 目标"装饰随导出件交付、在线编辑器所见即所得"**已实现并验证**：pptx 投屏件含封面/内容装饰，且与屏幕端同口径（无模糊、0.18 半透明）。
- 部署健康、两套 e2e 全绿（合计 43 断言 0 失败）。
- 本轮最关键产出是**发现并修复了 createImageBitmap 导致装饰无法嵌入 pptx 的真 bug**——此前该路径一直处于"看起来导出成功、实则装饰全丢"的静默失效状态。

---

# 附：第二轮（用户截图反馈「非全屏画布」三问题）2026-09-18 追加

## 六、问题与根因（全部实测取证，非推测）

用户在全屏编辑态截图反馈三点，实测后确认是**非全屏（嵌入）编辑态**的缺陷：

| # | 现象 | 实测根因 |
|---|---|---|
| ③ | 元件内容溢出画布 | 「课堂练习」页 4 元件 x=5/35/65/95、w=30，末位 **x+w=125% 越界落盘**（CW-EL）。几何写路径各自为政：属性面板输入只独立钳 0~100（不减去 w/h）、对齐·水平匀分布只保首末不校验边界、副本只钳 95 却不减 w。 |
| ① | 非全屏模式下（该元件）无 | 非全屏画布 960px 宽、右缘到 **1521px**（视口仅 1440），pane `overflow-x-hidden` 不可滚 → x=95% 的元件被裁在视口外完全不可见。根因是 **flex 收缩链断裂**：`PptxPreview` 根节点 `min-width:auto`，作为 pane（`flex justify-center`）子项被画布内容宽度撑住缩不下去，自适应算出的 scale 是自洽死锁（960=1280×0.75，永远"刚好等于自己"）。 |
| ② | 「隐藏页」非全屏无、且全屏位置不对 | 按钮原只存在于全屏顶栏、且被 `flex-1` 推到最右角（x=1370）与工具栏组脱节；非全屏只有极小的 ‹/› 图标、无文字按钮，不可发现。 |

## 七、修复（`PptxPreview.tsx` + `CoursewareBuilder.tsx`）

1. **统一几何钳制 `clampBox`**（新增模块级助手）：x∈[0,100-w]、y∈[0,100-h]，所有写路径收口——属性面板输入 `patchSel`、对齐 `alignSelected`、分布 `hdistribute/vdistribute`、副本 `duplicateSelected`；并**载入即钳界**（切页时 normalize 存档几何，若有变化则回写，使旧存档下次保存自愈）。
2. **宽度收缩链**：`PptxPreview` 根节点补 `w-full min-w-0`；非全屏/全屏画布 pane 补 `min-w-0 overflow-x-hidden`。→ 自适应 scale 基于真实可用宽，画布完整落在容器内。
3. **「隐藏页」统一**：并入 `renderToolbar`（两形态共用一个带文字的按钮，全屏控 `cwFsThumb`、非全屏控 `thumbCollapsed`）；删除全屏顶栏那个游离的最右角按钮与重复的 ‹ 图标。
4. **存量数据修复**：天窗 09-15_课件「课堂练习」页 4 元件重排为 x=3/27/51/75、w=22（界内等分）。

## 八、验证

- 新增守卫 `qa/verify_canvas_bounds.cjs`（**15 断言 0 失败**）：注入 x=95 越界元件 → 载入即钳回界内；非全屏画布右缘 1400 ≤ 视口 1440；元件全在画布内；「隐藏页」非全屏存在且可切换、全屏收在工具栏组内（x=601 < 720）；全程 pageerror=0。
- 回归 `qa/regression_20260917.cjs` 复跑 **37/0** 全绿（无破坏）。
- 实测几何：非全屏画布 682→1400（视口内），4 元件 704→1378 全在画布内；全屏 80→1360，元件 118→1322。

## 九、第二轮结论

用户三点反馈**全部修复并回归**；并已把"画布不溢出 / 元件不出界 / 隐藏页两形态可用"固化为常驻 e2e 守卫，防止再回归。

---

# 附：第三轮（用户指正「左右侧收起与展开被改回旧样」）2026-09-18

## 十一、我犯的错（对照证据）

用户曾花大量时间调校**左右对称、贴边收放**的一套闭环，且刻意做到与教案编辑器一致：

| 位置 | 调校好的形态 | 代码位置 |
|---|---|---|
| 左 | 工具栏**左端** `页面（N）/ + 页 / ‹` 收起页列表；收起后**左缘**浮出深色 `›` 贴边 tab | `:1202-1215`（本轮校正后） |
| 右 | 批注/版本面板自带收起；收起后**右缘**浮出灰色 `ChevronLeft` 贴边 tab，注释原话「与教案完全一致」 | `:1040-1045` |
| 全屏 | DECISIONS 09-14：*全屏态默认收起批注/版本栏与缩略图栏（让位画布）*；「隐藏页」在 `376e853`（全屏重写）原始设计里**就在工具栏内** | `DECISIONS.md:241`、`376e853` |

**我的错**（第二轮）：把左控件（页列表显隐）挪到了工具栏**最右端**、同时删掉了左端的 `‹` —— 把"控制左栏的按钮"放到了右侧，空间语义相反，等于退回"工具栏右端一个孤立按钮"的旧形态。这就是用户说的"走回去"。（画布 `min-w-0` / 几何钳制是独立的真 bug 修复，与左右收放无关，保留。）

## 十二、第三轮校正

1. **非全屏**：页列表显隐按钮**回到工具栏左端**（`页面（N）/ + 页 / 隐藏页|显示页` 同组），不再挂右端。
2. **全屏**：页列表显隐按钮**放回工具栏左端**（与非全屏同侧同位），不再游离最右角。
3. 右端（批注 + 右缘贴边 tab）与左缘贴边 `›` tab **一律未动**，调校好的闭环完整保留。

## 十三、第三轮验证

`qa/verify_canvas_bounds.cjs` 扩充为 **19 断言 0 失败**，新增守卫：

- 非全屏按钮位于左半区（x=586 < 720）· 全屏按钮位于左端（x=119）
- 展开态无左缘 tab → 收起后左缘浮出贴边 `›`（贴边闭环完好）→ 再展开后 tab 消失
- 画布右缘 1400 ≤ 视口 1440、元件全在画布内、pageerror=0

`qa/regression_20260917.cjs` 复跑 **37/0** 全绿。

### 第三轮收口（用户定稿：非全屏换回 ‹ 图标）

用户明确：非全屏左端换回 **`‹` 图标**（即调校好的原形态）。

- 非全屏：左端 `页面（N）/ + 页 / ‹`（图标版），收起后由**画布区左缘**浮出 `›` tab 再展开。
- 全屏：保持**文字按钮**「隐藏页/显示页」，位于工具栏左端。

守卫据此更新为 **18 断言 0 失败**：断言 `‹` 图标控件存在且在左半区（x=586）、收起后左缘贴边 `›` 浮出（x=466，位于画布左侧）、再展开后 `‹` 回归且贴边 tab 消失；全屏文字按钮位于左端（x=119）。`regression_20260917.cjs` 37/0。

---

# 附：第四轮（放映态「一键纯净」）2026-09-18

## 十四、现状核实（用户问：预览态没有一键全屏纯净版吗）

**确认：没有。** `CwPreviewPane.tsx:65-133`（查看态 = PPT 全屏播放/放映）三栏**硬编码常显**：
左 `w-44` 目录 + 中只读放映 + 右 `w-[260px]` 批注/版本（或 H5 扫码）—— 左右**都不可收起**，
更无"左右侧同时隐藏"。全屏**编辑**态虽有缩略图/批注收起，但那是编辑态、两个独立开关，也无"一键同时收"。

## 十五、主流交互调研（均有官方文档支撑）

| 产品 | 放映态做法 |
|---|---|
| 飞书幻灯片 | 演示态无侧栏；**鼠标移到左下角才浮出浮动工具栏**（翻页/演示者/激光笔）；快捷键 ←↑ 上一页、空格/↓/→ 下一页；另有演示者视图 —— 飞书官方帮助《演示幻灯片》《使用幻灯片快捷键》 |
| Google Slides | 放映态零 chrome；←/→ 翻页、S 备注、L 激光笔、**Esc 退出**；控制条鼠标触发才显、自动淡出 |
| 腾讯文档 / 金山 / 石墨 | 全屏演示 = 全屏无编辑面；**退出按钮鼠标移到顶部才出现**（自动隐藏）；Esc 退出 |
| PowerPoint / WPS | F5 放映零 chrome，工具栏静止数秒自动隐藏，Esc 退出 |

**三条共性**：① 放映 = **默认纯净**（不是再点一下才纯净）；② 左右侧/控制条**按需浮现 + 自动淡出**；③ **Esc 退出、←→空格翻页**。

## 十六、本轮落地（只加能力，不改既有默认）

`CwPreviewPane` 新增 `puri`（仅查看态/放映态传入）+ 一键纯净：

- **一键纯净**：右上角「⛶ 纯净放映」→ 左右侧**同时隐藏**、画布居中于深底（投屏更干净）、隐藏"预览模式（只读）"等编辑气息文案；再点或 **Esc** 还原。
- **悬浮控制条**：底部居中 `◀ 页码 ▶ | 退出纯净`，**鼠标静止 3 秒自动淡出**、移动即恢复（对齐飞书/Google Slides）。
- **键盘**：`←/→/↑/↓/空格` 翻页、`Esc` 退出纯净。
- **默认未改**：进入放映仍是原来的左右栏可见（避免擅自改动既有默认；是否改为"默认纯净"待拍板，见下）。

## 十七、第四轮验证

- 新增 `qa/verify_preview_pure.cjs`：**10 断言 0 失败** —— 入口存在 · 进入时左右栏默认显示 · 一键后左右**同时**隐藏 · 控制条浮出且页码正确 · 深底 · `→` 翻到 1/13 · `←` 翻回封面 · Esc 还原 · pageerror=0。
- 复跑既有三套：`regression_20260917` **37/0** · `verify_canvas_bounds` **18/0** · `verify_pptx_decor_export` **6/0**（`CwPreviewPane` 被 H5 编辑画布复用，已验证不受影响）。

## 十八、用户拍板与落地（2026-09-18 第五轮）

用户拍板：**① 可以默认纯净 ② 顶部标题栏也可自动淡出**。已落地：

1. **默认纯净**：放映态进入即纯净（左右侧同时隐藏、画布居中于深底），可一键「退出纯净」还原，或再次「⛶ 纯净放映」进入。
   - 例外：**H5 查看态保持原默认**（右栏是「扫码分享」= 交付入口，不是编辑 chrome）。
2. **标题栏沉浸淡出**：`PreviewOverlay` 新增可选 `dimChrome`（默认 false，其他编辑器不受影响），
   经 `EditorLayout.previewDimChrome` 由课件查看态传入；纯净中鼠标静止 3s **标题栏与控制条一起淡出**
   （`pointer-events:none` 防误点），鼠标移动即浮现。

### 过程中发现并修掉的真 bug（双实例串扰）

查看态下页面**同时挂了两份 `CwPreviewPane`**（背后布局的 `secondaryRight` 一份 + 放映外壳 `previewSlot` 一份，同一个 `previewPane` 元素）。
两份都装全局 `mousemove/keydown` 监听、都上报沉浸态 → 在外壳里「退出纯净」后，背后那份仍上报 `pure=true`，
会让**标题栏被误淡出**；键盘也会被两份同时处理。
修复：只有**位于放映外壳（`fixed inset-0`）内**的实例才接管键盘监听与沉浸态上报（`rootRef` 向上探测判定）。

## 十九、第五轮验证

- `qa/verify_preview_pure.cjs` 扩为 **17 断言 0 失败**：进入即纯净 · 左右同时隐藏 · 控制条 3s 自动淡出 ·
  **标题栏同步淡出（opacity 0 / pointer-events none）** · 移动即浮现 · `→` 1/13、`←` 回封面 ·
  一键退出还原 · 再进入再隐藏 · Esc 还原 · pageerror=0。
- 复跑既有三套：`regression_20260917` **37/0** · `verify_canvas_bounds` **18/0** · `verify_pptx_decor_export` **6/0**。

---

# 附：第六轮（staging 清空存量课件 + 守卫自建基线件）2026-09-18

## 二十、执行内容

1. **前端更新到 staging**：`qa/build_deploy.sh` → DEPLOYED。
2. **清空存量课件**（用户指令）：后端**无** `DELETE /api/materials/:id`，故走 SQL（SSH + `psql`，事务内）。
   - 精确定位 `category='courseware' AND type='courseware'` = **24 条**（10 draft ppt / 9 draft h5 / 3 active ppt / 1 active video / 1 active h5），全部属 sch-0001 / u-teacher。
   - **先备份**：`backups/staging_courseware_wipe_20260918/backup_materials_ann_ver.csv`（1.7M，含课件全字段 + 其批注 1 条 + 版本 36 条）。
   - 事务：先删引用（annotations / versions），再删课件本体 → **24 → 0**。
   - **未误伤**：`decor_element` 素材库 25 条原样保留。
3. **守卫改为「随用随建」基线件**（否则清库后 4 套 e2e 全废）：新增 `qa/lib/cwFixture.cjs`，
   按 `(name, format)` 找 `__E2E基线_课件` / `__E2E基线_H5课件`，没有就用 `POST /api/materials/json` 现建；
   4 套脚本改为经它取基线件（回归脚本同时把 H5 快照与内容同源对齐后才断言）。

## 二十一、清库后实跑验证（全绿）

| 脚本 | 结果 |
|---|---|
| `verify_canvas_bounds` | **19/0**（自动建 PPT 基线件，含课堂练习页 + CW-EL） |
| `verify_pptx_decor_export` | **6/0**（`ppt/media` 含 2 张真实图片） |
| `verify_preview_pure` | **18/0** |
| `regression_20260917` | **38/0**（基线件 + H5 快照对齐后全过） |

## 二十二、遗留与待确认

1. **测试会留下 2 条基线件**（`__E2E基线_课件`、`__E2E基线_H5课件`，draft）——因后端无删除接口，脚本无法自清理。
   命名带 `__E2E基线_` 前缀便于识别；SQL 清理：`DELETE FROM materials WHERE name LIKE '__E2E基线_%'`。
   若要"库里永远零课件"，需给后端加 `DELETE /api/materials/:id`（改动需你点头）。
2. **9 条上传附件**（`type=audio/doc/image/link/pdf/ppt/video/xlsx/lesson_plan`，如 `课文音频.mp3`、`拓展阅读链接`、`《观潮》教案（源头链路 2026-09-14）`）
   不在"课件"口径内，**未删**；是否一并清空待你确认。
3. 库内另有 **2 条批注 / 14 条版本孤儿**（引用早已不存在的材料），与本次无关、界面不展示，未处理。

---

# 附：第七轮（用户拍板 B1/B2 + B3 取证）2026-09-18

## 二十三、B2 落地：H5 查看态同样默认纯净

用户复核「H5 也可纯净」，取消原先"扫码栏默认可见"的例外：

- `CwPreviewPane`：`useState(!!puri)`（原 `puri && cwFormat !== 'h5'`）→ PPT / H5 一致。
- 扫码分享仍可用：点「退出纯净」即浮现右栏（手机扫码查看）。
- 守卫 `verify_preview_pure.cjs` 扩为 **21 断言 0 失败**，新增 H5 段：进查看态即纯净（扫码栏隐藏）→ 退出纯净后扫码栏浮现。
- 复跑其余三套：`regression_20260917` **38/0** · `verify_canvas_bounds` **19/0** · `verify_pptx_decor_export` **6/0**。

## 二十四、B1 落地：清空 staging 课件类数据

用户拍板「清掉」（含 9 条上传附件）：

- 删前备份：`backups/staging_courseware_wipe_20260918/backup_b1_final_category_courseware.csv`（91K，含 11 条全字段 + 引用）。
- 事务清理：`DELETE FROM materials WHERE category='courseware'` → **courseware 11 → 0**；`decor_element` **25 条完好**。
- 注：4 套守卫现在是「随用随建」基线件 —— **下次跑测试会再建 2 条 `__E2E基线_*`**（后端无删除接口，无法自清理；如需"永远零课件"需加 `DELETE /api/materials/:id`）。

## 二十五、B3 取证与建议（待用户拍板，未擅自改）

**问题**：`layout=edu-cover` 的页（在 `cwOutline` 内、且带老师加的元素）三端表现不一致：

| 端 | 行为 | 证据 |
|---|---|---|
| 编辑器画布 | 元素**照常渲染**（`isCover` 只影响配色/底带） | `PptxPreview.tsx:1335,2133,2296` |
| 预览/放映 | 封面分支（`kind==='cover' \|\| lay==='edu-cover'`）**完全不画 elements** | `PptxPreview.tsx:804-824` |
| 导出 pptx | 封面分支只认 `s.kind==='cover'` → 该页走内容分支，元素**有画但皮肤是普通页** | `exportPptx.ts:1339-1345` |

**建议 = 方案 A**：把已确立的「元素层是唯一渲染源」（`PptxPreview.tsx:838-841` 注释即此原则）贯彻到封面版式页 —— 预览封面分支叠加元素层、导出端封面皮肤判定与预览对齐并叠加元素层，并补三端一致守卫。代价小、方向安全（看不见→看得见，不丢数据）。
**备选 B**：若产品坚持封面版式页不吃元素层，则须在编辑器禁用+显式提示、导出端同步不画（代价更大，且违背既有收敛原则）。

## 二十六、本轮仍挂账（我自己的欠账，未擅动）

- **A1** 全部改动仍未提交（8 改 + 7 新）。
- **A2** `verify_pptx_decor_export.cjs` 的 `mediaFiles` 把 `ppt/media/` 目录项也计入（口径偏松，可能掩盖"只有目录无图片"）。
- **A3** `Materials.tsx` 素材库预览带入封面装饰**无 e2e 覆盖**。

---

# 附：第八轮（方案 A 实施：封面版式页元素层三端一致）2026-09-18

## 二十七、改动（3 处代码 + 基线件 + 守卫）

用户拍板「继续实施 A」——把代码里已确立的「**元素层是唯一渲染源**」贯彻到封面版式页：

| 位置 | 改动 |
|---|---|
| `PptxPreview.tsx`（预览/放映） | 封面分支（`kind==='cover' \|\| lay==='edu-cover'`）在标题/页脚之后**叠加元素层**；跳过 `slotKey==='title'` 的元素（标题已由 `<h2>` 渲染，避免重复——与 `layoutElements` 的跳过规则呼应） |
| `exportPptx.ts`（导出 pptx） | ① 封面皮肤判定与预览对齐：`kind==='cover' \|\| s.layout==='edu-cover'`（此前只认 `kind`，导致 edu-cover 页被画成普通白底内容页）；② 该分支内**叠加元素层**（同样跳过 title 槽元素） |
| `qa/lib/cwFixture.cjs` | 基线件增加 `自证_封面版式` 页（`layout: edu-cover` + 带标记元素的 CW-EL）；并新增**内容漂移自动复位**（保证用例可复现，不动 status） |
| `qa/verify_cover_elements.cjs`（新） | 三端一致守卫：编辑器可见 · 预览/放映可见 · 导出 pptx 的 `ppt/slides/*.xml` 含该元素文本 · 该页标题未丢 · pageerror=0 |

## 二十八、验证（全绿）

- `verify_cover_elements` **10/0**（三端都看得到 `封面自证元素`；导出 XML 长度 19173 且含标记与页标题）
- 复跑全部既有守卫：`regression_20260917` **38/0** · `verify_canvas_bounds` **19/0** · `verify_pptx_decor_export` **6/0** · `verify_preview_pure` **21/0**
- 合计 **94 条断言 0 失败**

## 二十九、副作用与说明

- 清库后跑守卫会重建基线件：本轮已重建 `__E2E基线_课件`（含 3 页：学习目标 / 课堂练习 / 自证_封面版式）与 `__E2E基线_H5课件`。
- 合成封面（`slides[0]`，不在 `cwOutline`、只读、无元素）不受影响；只有历史/手工标 `edu-cover` 且带元素的页行为发生变化，方向是「看不见 → 看得见」，**不丢数据**。

---

# 附：第九轮（A2/A3 收口 + C1~C6 处理）2026-09-18

## 三十、A 类（本轮欠账）收口

| 项 | 处理 |
|---|---|
| **A2** | `verify_pptx_decor_export.cjs` 的媒体计数收紧为**只数带扩展名的真实图片**（目录项不再计入），断言提升为 **≥2 张**（背景衬底 + 角标）。复测：`mediaNames=[image-1-1.png, image-1-2.png]` |
| **A3** | 新增 `qa/verify_materials_preview_decor.cjs`（**8/0**）：素材库列表 → 目标卡片「播放」→ 预览里 **注入前无装饰 / 注入后出现 data:image 背景** → 回滚。真实证明 `Materials.tsx` 的 `parseCoverDecor(content)` 生效。（过程中修掉守卫自身的过宽行匹配：原先 `innerText.includes(name)` 会命中整列，误点到另一张卡片） |
| **A1** | 全部改动统一提交（见 §三十三） |

## 三十一、C 类（预存缺陷/待办）处理

| 项 | 结果 |
|---|---|
| **C1 高**：参照课件拉取失败静默丢基线 | **已修**：`loadRefOutline` 失败 → `toast('参照课件加载失败，已跳过基线内容…','error')` + `console.warn`；参照课件下拉失败同样显式提示（此前恒空且无提示） |
| **C2**：三页「年级顶替班级」 | **已修**：`ExamBuilder` / `ExerciseGenerator` 改为与课件/教案/题单一致的三级口径（选中班级 → 主班级 → 首个任教班级 → **留空**），不再用年级兜底；`AssignmentBuilder` 信息卡「班级」只放班级名（卷面头部保留「年级 · 班级」格式，属对外卷面，不在本次口径） |
| **C3**：知识点单元统一读后端 | **部分收口 + 剩余有明确前置**。① 已修同处的**真缺陷**：预选此前按 `n.subject/n.grade/n.semester` 过滤，而这些字段在 `tb_kg_node` 里根本不存在（实测节点字段仅 `id,name,unit,version_id,level,parent_id,parent_name,prerequisites`）→ 过滤恒空 → 落到「全库前 6 个节点」，与学科/年级无关。现改为按**同一教材版本（version_id）**预选，口径自洽。② 剩余部分**卡在后端**：`/api/ai/knowledge/units` 只有 version_id 维度（无年级/学期），而 `/api/me/textbook-effective` 只返回 `{subject, publisher, version_name, source_level}`，**不给 kg 的 version_id** → 前端无法把单元接口 scoped 到当前教材。前置方案：后端给 units/nodes 加 `grade/semester` 过滤，或在 effective 响应里回传 kg version_id（二者均需后端改动 + 部署） |
| **C4-V1** | **已核实（非全量）**：`/api/ai/knowledge/nodes` 按 `limit` 截断（默认 300，实测 limit=1000 返回 1000 条），且返回的节点**混合 28 个教材版本**（也无 subject/grade）—— 这正是 C3 口径问题的根因 |
| **C4-V2** | **已核实**：`/var/www/school1.ziwi.cn/knowledge-graph.json` **存在**（56KB，随 dist 部署、前端 `fetch('/knowledge-graph.json')` 会读到）；`textbook-math.json` **不存在**（源码 `public/` 里也没有）→ 单元下拉的静态路径是**死的**（`textbookData` 恒 null → `currentUnits` 恒空） |
| **C4-V3** | **判据已定 + 已量化**：① 裸 `except:`（无类型）= **0 处**（达标）；② `except Exception` = 16 文件 92 处，绝大多数带 `logger.warning/exception`；`body={}` 类属**输入容错**可留；真·静默（`pass`/无记录）约 5~8 处，集中在 `api_server.py`，需逐一评估后拆（未擅动） |
| **C5**：孤儿批注/版本 | **已清**：`annotations` 2 → **0**、`versions` 14 → **0**（SQL 事务，条件为"resource_type ∈ (material,courseware) 且 materials 中无对应行"） |
| **C6**：G6 知识图谱告警 | **已闭环（静态 + 运行期双证）**：`useKnowledgePicker` 返回 `useMemo`（引用稳定）、三页均 `useEffect(() => setKGPicker(...))`；对 staging 跑 `verify_knowledge_graph_fix.cjs` → **depth=0 / destroyed=0 / draw=0 → PASS** |

### 附带发现（记入，未处理）

- `verify_knowledge_graph_fix` 运行中出现 1 条**非图谱类**错误：`[notifyError] 教材版本解析失败 TypeError: Failed to fetch` —— 说明某页在（可能未就绪的）鉴权/网络时机调用了教材版本解析且被显式报错（这是"拆静默"生效后的可见收益）。属独立小项，建议后续单独核对调用时机。

## 三十二、回归（本轮改动后全绿）

`regression_20260917` **38/0** · `verify_canvas_bounds` **19/0** · `verify_pptx_decor_export` **6/0** · `verify_preview_pure` **21/0** · `verify_cover_elements` **10/0** · `verify_materials_preview_decor` **8/0** · `verify_knowledge_graph_fix` **PASS** —— 合计 **102 条断言 0 失败**。

## 三十三、提交清单（本轮统一提交）

**前端源码**：`exportPptx.ts`（装饰导出/背景无模糊/CW-COVER 合成/Image 光栅化/封面版式元素层与皮肤对齐）· `PptxPreview.tsx`（几何钳制与载入自愈/宽度收缩链/封面分支叠加元素）· `CoursewareBuilder.tsx`（画布 pane 收缩/页列表开关归位/放映沉浸态/C1 拆静默）· `Materials.tsx`（素材库预览带 coverDecor）· `CwPreviewPane.tsx`（一键纯净+默认纯净+H5 一致+双实例隔离）· `PreviewOverlay.tsx`（`dimChrome`）· `EditorLayout.tsx`（`previewDimChrome`）· `ExamBuilder.tsx`/`ExerciseGenerator.tsx`/`AssignmentBuilder.tsx`（班级口径）· `useKnowledgePicker.ts`（预选口径）

**测试**：`regression_20260917.cjs`（陈旧断言修正 + 自建基线件）· 新增 `lib/cwFixture.cjs`、`verify_canvas_bounds.cjs`、`verify_pptx_decor_export.cjs`、`verify_preview_pure.cjs`、`verify_cover_elements.cjs`、`verify_materials_preview_decor.cjs`

**文档/备份**：本报告 · `backups/staging_courseware_wipe_20260918/`（两次清库的 CSV 备份，**留在本地未入库**，如需入库再提交）

> 提交：`2d76780`（19 文件 / +1380 −78），已 push `origin/main`（`94d7b4b..2d76780`）。

## 三十四、本轮结束时仍开放（需决策或后端改动）

1. **C3 剩余**：单元列表读后端 —— 前置=后端给 units/nodes 加 `grade/semester` 过滤，或在 effective 响应回传 kg `version_id`（需后端改动 + 部署）。
2. **C4-V3 残留**：`api_server.py` 约 5~8 处 `except Exception: pass` 需逐一评估（属 Python 服务，改动需后端部署）。
3. **B1 相关的自清理**：后端无 `DELETE /api/materials/:id` → 跑 e2e 会留下 `__E2E基线_*`（当前 2 条）；要"库里永远零课件"需加该接口。
4. **小项**：`教材版本解析失败 TypeError: Failed to fetch`（调用时机/鉴权就绪前触发，独立核对）。
5. `backups/` 两个 CSV 是否入库待定。

---

# 附：第十轮（继续处理开放项：C3 端到端 + C4-V3 拆静默 + Item 4 判定）2026-09-18

## 三十五、C3 端到端落地（单元/节点按教材收口）

**前置取证**：`tb_textbook_version` 有 `id(bigint) / xue_ke / nian_ji / ce_bie / ban_ben_biao_shi`，与 `tb_kg_node.version_id` **同源**；实测 `语文/四年级/上册 → version_id=1822（89 节点 / 8 单元，含"观潮"）`。

| 层 | 改动 |
|---|---|
| 后端 `kg_store.py` | 新增 `lookup_version_id(subject, grade, volume)`：按 学科/年级/册别 解析 `tb_textbook_version.id`；**解析不到返回 None**（调用方回退旧行为，避免把编辑器预选打断） |
| 后端 `api_server.py` | `/api/ai/knowledge/nodes` 与 `/units` 新增 `subject/grade/volume` 参数；未传 version_id 时先解析；响应回传 `version_id` 便于溯源 |
| 前端 `useKnowledgePicker.ts` | ① 请求带 `subject/grade/volume`（由 `teaching` + `GRADE_NAMES` + 学期映射 上册/下册）；② **单元列表改读后端**（静态 `textbook-math.json` 已确认不存在 → 旧路径恒空）；③ 节点回填真实 `subject/grade`；④ 单元预选与切换从已加载节点反查该单元节点（≤6）；⑤ 预选口径（上一轮已修）保持同版本 |

**效果（实测）**：带上下文后 `nodes` **只来自单一版本 1822（89 条）**（此前混合 28 个版本）；`units` 返回 8 个可读单元；**教案新建页单元下拉由「恒空」变为 9 项**（"请选择" + 8 单元）。

## 三十六、C4-V3 拆静默（8 处主链路异常可见化）

`except:` 裸用 = 0（达标）；本轮把**主链路上的静默**改为可见（`body={}` 类输入容错保留）：

1. 素材库读取失败（素材挂载整段失效）→ `logger.warning`
2. 素材挑选 LLM 失败回退启发式 → warning（留痕"为什么挂这几个"）
3. **教材知识边界检索失败**（生成悄悄失去知识面约束）→ warning
4. 小微锚点解析失败（anchor_coverage 失真）→ warning
5. 课标映射失败（课标对齐整块为空）→ warning
6. 教案关联素材挂载失败 → warning
7. 课件"相似课件"检索失败 → warning
8. 发散地图解析失败（轨道/边缘整块消失）→ warning

## 三十七、Item 4 判定（教材版本解析 Failed to fetch）

**判定为偶发（非缺陷）**：临时探针对 staging 实测 `/lesson-plans/new` 上该请求 **200 正常**、无报错复现；且失败已被显式上报（`TeachingContext:296` 的 `notifyError`）——正是"拆静默"的收益。复跑 KG 守卫本轮 `other: 0`，与判定一致。

## 三十八、验证（本轮）

- 新增 `qa/verify_kg_units_backend.cjs`：**10/0** —— 单元按教材返回（8 个，全可读名）· 回传 version_id · **节点只来自单一版本** · 节点单元 ⊆ 单元接口 · 解析不到时回退不返空 · **教案页出现单元下拉（9 项）** · pageerror=0
- 全量复跑：`regression` 38/0 · `canvas_bounds` 19/0 · `pptx_decor` 6/0 · `preview_pure` 21/0 · `cover_elements` 10/0 · `materials_preview_decor` 8/0 · `knowledge_graph_fix` PASS —— 合计 **112 条断言 0 失败**
- 部署：`code/deploy/deploy.sh staging`（前端构建 + backend/ai-service 同步 + 迁移 + 容器重建）

## 三十九、收尾时仍开放

1. **`DELETE /api/materials/:id`**（需你点头）：加它才能让 e2e 自清理（当前跑测试会留 2 条 `__E2E基线_*`）。
2. **C4-V3 剩余低危项**：`api_server.py` 中文件/JSON 读取类 `return ""/2` 的兜底（非主链路，未动）。
3. **前端静态 `textbook-math.json` 可删**（已确认无引用价值、且从未部署）；删它属清理动作，未擅动。
4. `backups/` 两个 CSV 是否入库。




## 十、提交清单（本次待提交，含第一轮）

- `code/frontend/src/lib/exportPptx.ts`（装饰导出 + 背景无模糊 + CW-COVER 合成 + Image 光栅化修复）
- `code/frontend/src/pages/Materials.tsx`（素材库 PPT 预览带 coverDecor）
- `code/frontend/src/components/PptxPreview.tsx`（clampBox 钳界 + 载入自愈 + 宽度收缩链）
- `code/frontend/src/pages/CoursewareBuilder.tsx`（pane min-w-0 + 隐藏页并入工具栏）
- `qa/regression_20260917.cjs`（2 条陈旧 blur 断言修正）
- `qa/verify_pptx_decor_export.cjs`（新增 · 装饰导出守卫）
- `qa/verify_canvas_bounds.cjs`（新增 · 画布边界守卫）
- 本报告 `QA_课件装饰导出回归_20260918.md`

