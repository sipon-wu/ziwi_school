# 知微AI教学助手 — 生产全页面冒烟测试报告

> 测试日期：2026-07-08
> 环境：**生产 school.ziwi.cn**（HTTPS）
> 账号：语文教师 `13800000002` / `teacher123`
> 方法：Playwright（Chromium headless）自动登录 → 遍历 App.tsx 全部路由 → 捕获 `console error` / `pageerror` / 白屏检测 + 每页截图；列表页因已统一改为 `window.open` 新标签（DOM 无 `<a href>`），改用 API 取真实 id 补测详情页（编辑器加载数据态）

---

## 一、结果汇总

| 类别 | 状态 | 数量 | 路由 |
|------|:----:|:----:|------|
| 简单路由（21 页） | OK | 19 | /teacher、/lesson-plans/new、/materials、/exams/new、/assignments/new、/analytics、/grading、/growth、/parent-sign、/published-lessons、/review-pool、/classes、/settings、/principal、/lesson-plans、/exercises、/exams、/assignments、/care |
| 简单路由（21 页） | **FAIL** | 1 | `/exercises/new` |
| 简单路由（21 页） | WARN | 1 | `/it-admin`（403 权限，正常，见三） |
| 详情页（4 页） | OK | 3 | /lesson-plans/:id、/exams/:id、/assignments/:id |
| 详情页（4 页） | **FAIL** | 1 | `/exercises/:id` |
| 关怀详情 | OK | 1 | /care（列表 OK；详情为弹窗交互，点击无崩溃） |

**总计 26 次访问：FAIL 2（同一根因）、WARN 1（正常权限）、其余 OK。**

---

## 二、P0 缺陷：出题模块知识图谱崩溃

- **现象**：`/exercises/new` 与 `/exercises/:id` 右侧知识图谱区抛出未捕获异常，页面部分崩溃（出题生成器主体可用，但图谱组件报错）。
- **错误**：`Cannot read properties of undefined (reading 'draw')`
- **根因定位**：`code/frontend/src/components/KnowledgeGraph.tsx`
  - 依赖 `@antv/g6` `^5.1.1`（**G6 v5**），组件整体使用 v5 API：`new Graph(...)`（158/202 行）、`graph.render()`（197/244）、`graph.setElementState()`（283）、`graph.getNodeData()`（278）、`graph.setSize()`（265/294）、`graph.destroy()`（268/270）。
  - **唯独 284 行 `graph.draw()` 是 G6 v4 遗留 API**。v5 的 `Graph` 实例无 `.draw()` 方法；该调用（或触发 G6 内部重绘）在实例未就绪时序下访问 undefined 的 `draw`，且发生在 G6 内部异步渲染路径中，绕过外层 `try/catch`（277–287 行），最终成为 `pageerror`。
- **影响范围**：仅出题模块（`ExerciseGenerator` 右侧 `KnowledgeGraphTool`）。题库列表、试卷、作业、教案、关怀、学情、设置等模块均不受影响。
- **关联 404**：`/exercises/new` 另有 1 个 `404` 资源（疑似某静态资源，非阻断，建议另行排查）。

---

## 三、权限控制验证（正常，非缺陷）

- `/it-admin` 返回 `403`：教师账号无权访问 IT 管理接口，权限控制生效。WARN 仅因该 403 资源被控制台记录，**不计为缺陷**，可降为 info。
- `/principal` 仅渲染 ~30 字符：教师访问校长页显示极简受限页，无崩溃，权限控制生效。

---

## 四、历史缺陷回归验证

- `/lesson-plans/:id`（编辑器加载已有数据态）→ **OK**，确认前一轮"白屏回归 bug（`useNavigate` 误删导致）"已修复、**无回归** ✓

---

## 五、漏测与限制

1. **侧边栏导航「模拟点击」覆盖不全**：选择器（`nav/aside/[class*=sidebar]`）未命中自研侧边栏结构，仅成功触发 1 次导航。但全部路由已通过直接 `goto` 覆盖，导航交互建议人工目视确认。
2. **/care 详情为弹窗交互**（非路由 `/care/:id`），已点击验证无崩溃，弹窗完整内容建议人工目视。
3. **截图不可自动判读**：当前模型不支持读取图片，白屏判定依据为 DOM 文本 + `pageerror`；关键截图存于 `/tmp/qa/shots/` 供人工查看。
4. **详情页取数方式**：列表页已统一改为 `window.open` 新标签，DOM 无 `<a href>`，故详情页改用后端 API 取真实 id 访问，确保测的是「加载数据态」而非空态。

---

## 六、修复建议（待拍板）

**方案 A — 止血（低风险）**
删除 `KnowledgeGraph.tsx:284` 的 `graph.draw()`。G6 v5 中 `setElementState(states)` 已触发状态重绘，无需手动 `draw`（该行是 v4 遗留）。预计直接消除 `/exercises/new` 与 `/exercises/:id` 两处 `pageerror`。

**方案 B — 根治（中风险）**
在 A 基础上，统一 G6 v5 API 并加健壮性：核对全部图表调用；为图谱容器/实例创建加空值守卫（如 `containerRef.current` 为空时跳过 `new Graph`，避免容器未挂载时异常）。需本地 `vite build` + Playwright 回归 `/exercises/*` 确认高亮与渲染正常。

**待你拍板**：是否修复、采用 A 还是 B。
