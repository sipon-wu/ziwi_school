# 文档评审记录 · 2026-09-16（会话决议回填）

> 评审对象：`DECISIONS.md`（09-12~09-16 回填段）、`code/CHANGELOG.md`（09-12~09-16 段）、`产品规划/缺省规则.md`（R1–R3）
> 评审方式：**结合代码检查**（见下方协议）；高风险结论走**独立复核**（不带结论反证）
> 结论：**通过（附 3 条待办 + 6 条待核实）**

---

## 一、复核协议（长期方法）

1. 每条结论必须挂四类证据之一，否则**不得写 `[x]`**，只能 `[ ] 待核实`：
   ① 代码路径（`file:line` + 实际行为）② 数据/契约（迁移、模型、接口字段）③ 运行证据（staging 实测 + 可复现命令）④ 决议出处（规格条款）
2. 每条必须附**复核方法**（"下一个接手者怎么自己验"），禁止只写"已核查"。
3. 判定**三态**：`一致` / `部分`（写清已落与未落）/ `不一致`（附反例）。**禁止"约等于通过"**。
4. 会长期留在 `DECISIONS.md` 的高风险结论，必须由**不带原结论**的独立检查反证。
5. 复核记录留档；**截图不作证据**（`.gitignore` 已忽略 `qa/shots_*/`），只引文本与代码行。

---

## 二、独立复核结果（6 条高风险声明）

| 条目 | 判定 | 关键证据 | 处置 |
|---|---|---|---|
| 知识点数据源统一读 DB | **部分** | `useKnowledgePicker.ts:86` 节点读 `/api/ai/knowledge/nodes`；但 `:104-116` 静态 `knowledge-graph.json` 仍作兜底；`:118` **单元仍读静态** `textbook-math.json`，后端 `/knowledge/units` 前端零调用 | 措辞已改 + 待办 A |
| 拆静默 | **不一致（仍有残留）** | 约 20 处残留，最高危：`CoursewareBuilder.tsx:682` `materialAPI.get(genBaseId).catch(()=>null)`（生成链路参照课件失败静默丢基线） | 措辞已改为"进行中" + 待办 B |
| 元素层是唯一渲染源 | 一致（补例外） | `PptxPreview.tsx:783-787` 无 elements 时回退 `renderLayoutContent`；`:750` 封面分支**无条件优先** | 已补例外说明 |
| 班级准确性 | **部分** | 已改 3 页：`LessonPlanEditor:173`、`CoursewareBuilder:656`、`SheetBuilder:47`；**待改 3 页**：`ExamBuilder:43`、`ExerciseGenerator:192`、`AssignmentBuilder:91` | 措辞已改 + 待办 C |
| 列表点行行为 | 需修正描述 | `CoursewareList.tsx:296-304` 右侧是**一个按钮按状态二选一**（草稿→笔尖进 edit；已发布→眼睛进 view） | 措辞已修正 |
| 缺省规则三模块 | 一致 | `layoutLaw` 被 renderer/VisualBlocks/styleRegistry 消费；`textClean` 被 mdToStory/PptxPreview 消费；`versionPolicy` 被 CoursewareBuilder + 单测消费，**无死导出** | 可升格规格章 |

## 三、代码核验补充（4 条）

| 条目 | 核验结果 | 判定 |
|---|---|---|
| 流式 SSE | `lib/api.ts:246-277` 用 **fetch + ReadableStream**（注释说明为何不用 EventSource：**带不了 Authorization 头**）；调用点 `LessonPlanEditor.tsx:453` | 一致 |
| 溯源落库 | `scope_resolved` 命中 4 处：迁移 `0009_material_provenance.sql` → `material.go` → `api_server.py` → `CoursewareBuilder.tsx`（链路完整） | 一致 |
| 字号层级 / autofit / 标题拆分 | `TYPE_SCALE`、`splitTitle` 被 `exportPptx.ts` 与 H5 `renderer.ts` **同时 import**（两端同源，规则真被执行） | 一致 |
| 缺省规则是否被执行 | 同上（消费方全覆盖，无定义未用） | 一致 |

## 四、待核实（V1–V6）

| # | 待核实 | 复核方法 | 状态 |
|---|---|---|---|
| V1 | `/api/ai/knowledge/nodes` 运行时是否返回全量 | staging 调接口核对条数与 `version_id/unit` 字段 | 待核实 |
| V2 | `dist/`、备份产物里的 `knowledge-graph.json` 是否会被部署路径读到 | 查部署脚本 + nginx 静态根 | 待核实 |
| V3 | Python 侧宽泛 `except` 是否算"静默残留" | 先定判据（是否带标记/是否影响主链路），再逐文件核 | 待核实 |
| V4 | `classLabel` 的 ID 口径（`myClasses.class_id` ↔ `selectedClassId`）是否匹配 | 静态即证：`AppLayout.tsx:100` `toClassInfo` 写入 `id: item.class_id`，比较侧亦用 `class_id` → **口径一致** | **已核（静态）** |
| V5 | 封面页是否会被写入 elements | 合成封面不在 `cwOutline` 内 → 不可能有 elements；**但**模型若把"封面"当一页并标 `layout:edu-cover`（历史出现过），该页 elements 会被封面分支忽略 → **属产品行为待定**（edu-cover 页要不要吃元素层？） | 待产品定 |
| V6 | `pathname.includes('/edit')` 边界 | ID 为素材 UUID（仅 hex 与 `-`）→ 不成立，低风险 | **已核（静态）** |

## 五、门禁判定

| 检查项 | 结果 |
|---|---|
| 每条结论可在仓库内定位（file:line 真实存在） | ✔ 45/45 |
| 与现行规格无**未标注**冲突 | ✔（取代关系已在文档中显式写明） |
| 术语一致（AI 模式/文档模式/预览/元素层/缺省规则/工作单元） | ✔ |
| 归组正确（DECISIONS / CHANGELOG / 规格附录） | ✔ |
| 编号可追溯（台账号 ↔ 文档条目） | ✔ |
| 无"自作主张"：未拍板项一律 `[ ]` + 标注 | ✔ |
| 每条附复核方法 | ✔（DECISIONS 回填段为纲要版，逐条方法见本记录） |
| 无敏感信息（凭据/口令/学生个人信息） | ✔ |

**门禁结论：通过** → 可进入 git 提交（分支 `docs/session-backfill-0916`，push 前需用户确认 diff）。

---

## 六、待办（3 条，来自本轮评审）

| # | 待办 | 来源 | 建议优先级 |
|---|---|---|---|
| A | 知识点**单元**统一读后端（`/api/ai/knowledge/units` 前端零调用）；同时修 DB 节点 `subject:''/grade:0` 与预选过滤口径不一致 | 独立复核 #1 | 中（影响"知识点来源"体验） |
| B | 清理静默残留（约 20 处），**先修生成链路** `CoursewareBuilder.tsx:682`（参照课件失败静默丢基线） | 独立复核 #2 | **高** |
| C | 3 页班级口径对齐：`ExamBuilder:43`、`ExerciseGenerator:192`、`AssignmentBuilder:91`（仍在拿年级顶替班级） | 独立复核 #5 | 中（准确性） |
